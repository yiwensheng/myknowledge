# 易知 操作手册

> 自我进化个人知识库 · 易文胜开发 2026  
> 项目路径默认：`e:\app\Myknowledge`（英文名 Myknowledge）

---

## 1. 快速开始

### 1.1 图形界面（推荐）

双击 **`启动易知.bat`** 或 **`启动Myknowledge.bat`**：

1. 自动检查 Python 依赖（首次 `pip install`）
2. 自动检查 Electron 依赖（首次 `npm install`）
3. 自动初始化目录（首次 `yws init`）
4. 打开 GUI，后台启动 Python API（默认端口 `18765`）

界面右上角 **「关于」** 可查看版权信息。

### 1.2 命令行

将项目目录加入 PATH（可选）：

```powershell
setx PATH "%PATH%;e:\app\Myknowledge"
```

常用命令：

```powershell
cd e:\app\Myknowledge
yws init          # 初始化
yws ingest        # 整理 inbox
yws ask "问题"    # RAG 提问
yws list          # 列出条目
```

命令行完整参数见 **[CLI.md](CLI.md)**。

### 1.3 首次配置 LLM

复制并编辑环境变量：

```powershell
copy .env.example .env
```

必填项（示例）：

```ini
MYKNOWLEDGE_LLM_API_BASE=https://api.emmm204.site:4170/v1
MYKNOWLEDGE_LLM_API_KEY=你的密钥
MYKNOWLEDGE_LLM_MODEL=qwen3.6
```

未配置 API Key 时：**入库、查询、索引** 仍可用；**提问 / 产出** 无法调用 LLM。

---

## 2. 目录结构说明

```
Myknowledge/                  # 易知 · 项目根目录
├── inbox/              ← 【待处理】批量丢文件的第一站
├── concepts/           ← 概念、方法论、原理
├── entities/           ← 工具、产品、人物、机构
├── sources/            ← 外部资料摘要（网页、PDF 等入库后）
├── comparisons/        ← 对比分析
├── notes/              ← 一般笔记
├── archive/            ← 归档（含 inbox-processed 已处理原文件）
├── assets/             ← 原文件与媒体（documents/images/audio/video）
├── output/             ← produce 产出（未写入 wiki 时）
├── index/              ← 自动生成的索引页（勿手改）
├── templates/          ← 新建条目模板
├── .rag/               ← RAG 向量片段索引（自动生成）
├── purpose.md          ← 知识库研究方向与标签约定
├── prompts/            ← LLM 人设与提示词
├── electron/           ← GUI
├── backend/            ← API 服务
└── lib/                ← 核心逻辑
```

### 各目录用途对照

| 你要做什么 | 放哪里 | 之后执行 |
|-----------|--------|----------|
| 批量导入 PDF/Word/PPT 等 | `inbox/` | `yws ingest` 或 GUI「整理 inbox」 |
| 批量导入已有 Markdown | `inbox/` 或对应分类目录 | ingest / `yws index` |
| 抓取网页 | GUI「URL 入库」或 `yws url "https://..."` | 自动入库 + RAG |
| 手写概念笔记 | `concepts/` 或 GUI「列表」新建 | `yws index` |
| 保留原文件引用 | 自动进 `assets/`，正文进 `sources/` 等 | ingest 自动处理 |
| 只读外部文件夹参与 RAG | `.env` 配置 `MYKNOWLEDGE_EXTRA_DIRS` | `yws index` |

---

## 3. 批量导入文档（详细）

### 3.1 方式一：复制到 `inbox/`（最常用）

适合：**一次性导入大量混合格式文件**。

**步骤：**

1. 在资源管理器中打开 `e:\app\Myknowledge\inbox\`
2. 将文件**批量复制或拖入**该文件夹  
   支持：pdf · ppt/pptx · xls/xlsx · doc/docx · txt · md · png · jpg · mp3 · mp4
3. 执行整理：
   - **GUI**：「更新」Tab →「整理 inbox」
   - **命令行**：`yws ingest`
4. 系统自动：
   - 提取文本
   - 原文件存入 `assets/documents/`（或 images/audio/video）
   - 在 `sources/`（或 concepts 等）生成 Markdown 条目
   - 已处理的原文件移到 `archive/inbox-processed/`
   - 重建 `index/` 与 `.rag/`

**Windows 批量复制示例：**

```powershell
# 将 D:\资料库\2026Q1 下所有 PDF 复制到 inbox
Copy-Item "D:\资料库\2026Q1\*.pdf" "e:\app\Myknowledge\inbox\"

# 整理
cd e:\app\Myknowledge
yws ingest
```

### 3.2 方式二：GUI 上传

「更新」Tab → 选择文件 →「上传并入库」。  
等效于复制到 `inbox/` 并立即 ingest。

### 3.3 方式三：直接放入分类目录

若你**已是规范 Markdown**（含 frontmatter 更佳），可直接复制到：

- `concepts/` — 概念
- `sources/` — 资料来源
- `notes/` — 笔记
- …

然后执行 **`yws index`** 重建 RAG（无需 ingest）。

### 3.4 方式四：URL 入库

```powershell
yws url "https://example.com/article"
```

或 GUI「更新」→ 输入 URL →「URL 入库」。

- 本地化 Markdown：`assets/documents/urls/`
- 知识条目：`sources/`
- 同一 URL 默认去重（`.wiki-cache.json`）

### 3.5 方式五：外联目录（不复制，只参与 RAG）

见下文 **第 6 节**。

---

## 4. GUI 各 Tab 用法

| Tab | 功能 |
|-----|------|
| **提问** | 基于 RAG 检索 + LLM 流式回答；无库内依据时不胡答 |
| **查询** | 关键词或 RAG 片段检索 |
| **列表** | 浏览、新建、编辑、删除 wiki 条目 |
| **产出** | 根据库内资料生成新 Markdown，可选写入 wiki |
| **更新** | URL 入库、文件上传、整理 inbox、重建索引 |

**开着 GUI 时**，后台 watcher 会轮询 `inbox/` 等目录，新文件可自动 ingest（与 `yws watch` 类似）。

---

## 5. 典型工作流

### 5.1 新资料 → 提问

```
复制文件到 inbox/ → yws ingest → yws ask "这份资料的核心观点是什么？"
```

### 5.2 新资料 → 写成 wiki 条目

```
inbox/ → ingest → yws produce "主题总结" --wiki
```

### 5.3 手改 Markdown 后

```
Obsidian / 记事本编辑 *.md → yws index
```

### 5.4 长期后台自动整理

```powershell
yws watch
```

或保持 GUI 运行。

### 5.5 越用越聪明（记忆与自动归档）

易知 **不会微调大模型权重**，但通过以下机制让系统**越用越好用**：

| 机制 | 说明 | 默认 |
|------|------|------|
| **连续对话** | 同一会话内追问时带上此前问答（如「展开刚才第二点」）；**会话文件永久保留全部轮次** | 开 |
| **历史问答检索** | 自动检索过去有依据的问答，辅助理解意图 | 开 |
| **有依据自动归档** |  grounded 回答写入 `notes/qa/`，并 **轻量刷新 RAG**（`refresh_rag`，比全量 `yws index` 更快） | 开 |
| **ingest / produce --wiki** | 资料与产出持续写入库 | 手动触发 |

**存储位置：**

| 层 | 路径 | 作用 |
|----|------|------|
| **SQLite 索引（主）** | `.memory/memory.db` | 每轮对话 `turns`、问答日志 `qa_log`；历史 Tab / API 查询 |
| 会话备份 | `.memory/sessions/{session_id}.json` | 双写备份，便于人工查看 |
| 问答备份 | `.memory/qa-log.jsonl` | 双写 append-only 日志 |
| 知识归档 | `notes/qa/问答-*.md` | 有依据问答进入 wiki + RAG（标签 `qa-archive`） |

首次启动或执行 `python scripts/migrate_memory_db.py` 时，会自动把既有 JSON / jsonl 导入 SQLite。

**GUI「提问」Tab：**

- ☑ 连续对话 / ☑ 有依据时自动入库（勾选状态与 **会话 ID 写入 localStorage**，重启后自动恢复）
- 启动时从当前会话加载历史轮次，显示在回答区上方
- 「新对话」生成新 session_id 并清空当前展示

**GUI「历史」Tab：**

- 浏览 SQLite 中的问答记录（源数据同步自 `qa-log.jsonl`），可按会话筛选
- 选中记录查看完整问答；「继续此会话」切换 session_id 并回到「提问」Tab 追问

**命令行：**

```powershell
yws ask "第一个问题"
yws ask "那第二点呢？"              # 沿用 CLI 会话（.memory/cli-session.json）
yws ask "问题" --new-session       # 新会话
yws ask "问题" --no-remember       # 单次，不带上下文
yws ask "问题" --no-archive        # 不归档到 notes/qa/
```

**`.env` 可调：**

```ini
MYKNOWLEDGE_ASK_REMEMBER=1
MYKNOWLEDGE_AUTO_ARCHIVE_QA=1
MYKNOWLEDGE_MEMORY_TURNS=6    # 追问时注入 LLM 的最近 N 轮（0=全部；仅影响上下文，不删磁盘记录）
MYKNOWLEDGE_MEMORY_QA=3       # 注入几条历史相关问答
MYKNOWLEDGE_RAG_TOP_ASK=6      # 提问检索片段数（越大越全、略慢）
MYKNOWLEDGE_RAG_TOP_PRODUCE=8  # 产出检索片段数
```

**说明：**

- `.memory/memory.db` 与 `qa-log.jsonl` **不会**因轮数上限而删除旧记录；GUI「历史」可分页查看。
- `MYKNOWLEDGE_MEMORY_TURNS` 只控制**发给大模型**的最近几轮，默认 6 是为控制 token 用量；设为 `0` 则把该会话全部轮次都注入（长会话可能超上下文窗口）。
- RAG 索引带内存缓存；标题命中加权 1.35×；片段注入时标注相关度与检索方式（keyword / vector / hybrid / anythingllm）。
- **混合 RAG（默认）**：本地词频 + OpenAI 兼容 embedding（`.rag/embeddings.db`）；`yws rag status` 查看状态，`yws rag rebuild` 仅重建 RAG。
- **AnythingLLM 外联（可选）**：配置 `MYKNOWLEDGE_ANYTHINGLLM_*` 后可用 vector-search 增补，或 `CHAT_FALLBACK=1` 在本地无命中时借用 workspace query。
- 问答/产出入库后自动 `refresh_rag`；大批量改 wiki 后仍建议 `yws index`。

**迁移 / 重建索引库：**

```powershell
python scripts/migrate_memory_db.py
```

### 5.6 Skills（可扩展工作流）

易知 **支持 Skills**，格式与 **Cursor Agent Skills** 相同（`SKILL.md` + YAML 头）。

**内置 Skill（`skills/` 目录）：**

| Skill | 作用 |
|-------|------|
| wiki-ingest | 整理 inbox |
| wiki-index | 重建 RAG |
| wiki-review | 知识库回顾 |
| wiki-url | URL 入库 |
| wiki-ask | RAG 提问 |
| wiki-curator | 总控菜单 |

```powershell
yws skill list
yws skill show wiki-ingest
yws skill run wiki-ingest
yws skill run wiki-url --url "https://..."
yws skill match "整理知识库"
```

**GUI**：「Skills」Tab → 选择 → 执行。

**添加自定义 Skill：**

1. 新建 `skills/你的技能名/SKILL.md`（参考 `skills/README.md`）
2. 在 frontmatter 中设置 `myknowledge.action` 或提供 `scripts/run.py`
3. `yws skill list` 验证

**扫描路径：** 项目 `skills/` · `{WIKI_ROOT}/skills/` · `MYKNOWLEDGE_SKILLS_DIRS` · 默认含 `~/.cursor/skills/`

### 5.7 Loop Engineering（自主进化循环）

集成 [Loop Engineering](https://github.com/cobusgreyling/loop-engineering) 思想，模式 **knowledge-evolution**：

```
触发 → 发现 inbox/草稿/RAG 缺口 → 更新 STATE.md → L2+ 自动 ingest/index → L3 可选 produce
```

| 文件 | 作用 |
|------|------|
| `STATE.md` | 跨会话记忆脊柱（High Priority / Watch List） |
| `LOOP.md` | 循环配置与人机门禁 |
| `.loop/loop-run-log.jsonl` | 运行日志 |

```powershell
yws loop status
yws loop run              # 默认 L1（仅报告）
yws loop run --level 2    # 自动 ingest + index
yws loop watch --interval 3600
yws skill run loop-triage
```

**第一周**：默认 `MYKNOWLEDGE_LOOP_WEEK_ONE=1`，只更新 `STATE.md` 不自动改库。确认无误后设 `MYKNOWLEDGE_LOOP_WEEK_ONE=0` 再开 L2。

**与自动归档联动**：L1 发现「问答日志未归档到 notes/qa」时会写入 Watch List；开启 `MYKNOWLEDGE_AUTO_ARCHIVE_QA=1` 后，有依据问答会即时入库并刷新 RAG，减少该缺口。

**GUI**：「更新」Tab → **循环 L1** / **循环 L2**。

**可选审计**（需 Node）：`npx @cobusgreyling/loop-audit . --suggest`

---

## 6. 外联目录（不复制即入库 RAG）

### 6.1 是否支持？

**支持只读外联索引**（v2.1+）：文件**留在原处**，易知 **不复制**，但会读取内容并写入 RAG，供提问 / 查询使用。

**限制：**

- 外联文件**不会**出现在 GUI「列表」的 wiki 条目中
- 检索结果路径以 `@external:盘符/路径/...` 显示
- 修改外联文件后需 **`yws index`** 刷新 RAG
- 不支持对外联目录做 ingest（不会生成 sources 条目）

### 6.2 配置方法

编辑 `.env`：

```ini
# Windows：多个目录用英文分号分隔
MYKNOWLEDGE_EXTRA_DIRS=D:\公司文档\参考;E:\读书笔记

# 也可换行（每行一个路径）
```

保存后：

```powershell
yws index
```

### 6.3 备选：目录联接（整库外置）

若希望**整个知识库**都在别的盘：

```powershell
# 方式 A：环境变量（推荐）
setx WIKI_ROOT "D:\MyWiki"

# 方式 B：NTFS 目录联接，把 inbox 指到外部队夹
mklink /J "e:\app\Myknowledge\inbox" "D:\待整理资料"
```

方式 B 下，复制到 `D:\待整理资料` 等效于复制到 `inbox/`，会走完整 ingest（含复制到 assets）。

---

## 7. Obsidian 协同

用 Obsidian **打开文件夹** `e:\app\Myknowledge` 即可编辑正文。

- **易知 / yws** 负责：ingest、RAG、`index/` 自动生成
- **你**负责：编辑 `concepts/`、`notes/` 等正文
- 手改后执行 **`yws index`**

`index/*.md` 由脚本生成，请勿手改。

---

## 8. 数据库：是否需要？SQLite 还是 PostgreSQL？

### 8.1 结论：**当前不需要数据库**

易知 采用 **「Markdown 文件 + JSON 索引」** 架构：

| 数据 | 存储方式 |
|------|----------|
| 知识正文 | `*.md`（Obsidian 可读） |
| RAG 片段 | `.rag/chunks.json` |
| 资产清单 | `assets/manifest.json` |
| URL 去重 | `.wiki-cache.json` |
| 索引页 | `index/*.md` |

对个人本地知识库，这足够简单、可备份、可 Git 管理。

### 8.2 若未来要加数据库

| 场景 | 建议 |
|------|------|
| 单机个人使用、条目 &lt; 10 万 | **SQLite**（零运维，单文件备份） |
| 多用户、远程 API、与 XDFAI/LearnMate 共用 | **PostgreSQL** |
| 仅加速全文检索 | 可先上 **SQLite FTS5**，不必一步到位 PostgreSQL |

**不建议**为当前版本引入 PostgreSQL：增加部署复杂度，而收益有限。

---

## 9. 维护与故障排除

| 现象 | 处理 |
|------|------|
| GUI 显示后端未连接 | 检查端口占用；`.env` 中改 `MYKNOWLEDGE_PORT` |
| 提问无回答 / 胡说 | 确认 API Key；先 `yws query 关键词 --rag` 看是否有命中 |
| ingest 后搜不到 | 执行 `yws index` |
| 旧版 .doc/.ppt 提取差 | 转为 docx/pptx 再放入 inbox |
| 端口被占用 | `MYKNOWLEDGE_PORT=18766` 后重启 |

---

## 10. 相关文档

| 文档 | 内容 |
|------|------|
| [README.md](README.md) | 项目概览 |
| [CLI.md](CLI.md) | 命令行完整参考 |
| [purpose.md](purpose.md) | 知识库研究方向 |
| [prompts/](prompts/) | LLM 人设与提示词 |

---

*易知 · 易文胜开发 2026*
