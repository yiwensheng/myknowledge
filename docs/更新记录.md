# 易知 · 更新记录

> **规则（全局）**：测试成功的改动 → `docs/更新记录.md` 顶部追加；**确认版本迭代** → 同步 `docs/commercial/changelog.html` 并上传服务器。Agent 遵守 `.cursor/rules/yizhi-update-log.mdc`。  
> **排版**：条目用 `####` 标题；子项用加粗标签单行/短段，少空行。  
> 当前版本 `electron/package.json` **3.0.2**（**2.1.1 为失败版本，已撤回**）。

---

#### 2026-08-13 17:07 — 发布 3.0.2（外联目录添加修复）· 已构建

**改了什么**：升至 3.0.2；打入外联配置改写 wiki 可写路径的修复；同步 changelog、latest.json、安装向导与闲鱼下载链。  
**为什么改**：用户反馈添加外联目录 Internal Server Error；确认发补丁版。  
**涉及文件**：`package.json`、`YizhiSetup.iss`、`installer-highlights.txt`、`changelog.html`、`latest.json`、闲鱼手册等。  
**改后效果**：本地产物 `dist\Yizhi-Setup-3.0.2.exe`（约 439 MB）；须压成 zip 后上传 `yizhi/`：zip、`latest.json`、`changelog.html`。  
**发版**：是 → 待上传服务器三件套。

---

#### 2026-08-13 16:20 — 修复外联目录添加 500（配置改写 wiki）· 已测试

**做了什么**：`linked-dirs.json` 改为写入知识库目录 `wiki_root/.config/`（可写），不再依赖安装目录 `ROOT/.config`；首次自动从旧路径迁移；`.env` 同步失败不再拖垮添加；添加 API 对监视器/索引启动失败降级。  
**为什么**：安装到 Program Files 等只读位置时点「添加目录」会 PermissionError → 前端 `Error: Internal Server Error`。  
**涉及文件**：`lib/linked_dirs.py`、`backend/server.py`、`tests/test_linked_dirs_config.py`。  
**改后效果**：`pytest tests/test_linked_dirs_config.py` 2 passed。  
**发版**：否，建议打进下一安装包（如 3.0.2）；开发态重启后端即可验。

---

#### 2026-08-13 16:10 — 头条「去 AI 味 10 Skills」评估与三处落地 · 已测试

**做了什么**：评估[头条文](https://www.toutiao.com/article/7673332939782029858/)后**不全装**；全局安装 `humanizer-zh`、`shuorenhua`、`de-ai-writing`；「我读史记 / Articles」规则默认叠加；易知写文章注入增强包（`MYKNOWLEDGE_DE_AI_WRITING`，默认开）。  
**为什么改**　十套重叠会打架；互补三项覆盖中文痕迹、说人话、生成硬门槛。  
**涉及文件**：`.cursor/rules/wodu-shiji*`、`articles-hotspot*`、`Articles/00-写作规则.md`、`lib/de_ai_writing.py`、`lib/prompts.py`、`lib/settings.py`、`skills/de-ai-writing/`、`tests/test_de_ai_writing.py`、评估说明、vendor 三仓。  
**改后效果**：`pytest tests/test_de_ai_writing.py` 通过；写稿无需点名即叠三 Skill；易知设置可关增强包。  
**发版**：否，随下一安装包。

---

#### 2026-08-13 13:56 — 发布 3.0.1（自动检查更新进安装包）· 已构建

**改了什么**：升至 3.0.1；打入启动自动检查更新、测试连接/进度条开关等 3.0.0 后修复；同步 changelog、latest.json、安装向导与闲鱼下载链。  
**为什么改**：用户要求更新安装包。  
**涉及文件**：`package.json`、`YizhiSetup.iss`、`installer-highlights.txt`、`changelog.html`、`latest.json` 等。  
**改后效果**：本地产物 `dist\Yizhi-Setup-3.0.1.exe`（约 439 MB）；须压成 zip 后上传 `yizhi/`：zip、`latest.json`、`changelog.html`。  
**发版**：是 → 待上传服务器三件套。

---

#### 2026-08-13 11:55 — 启动时自动检查新版（每进程一次）

**做了什么**：连上后台后延迟约 2.5s 自动调 `/api/update/check`；有新版或当前版已撤回时弹确认框，可一键打开下载页；无网/已最新静默；`autoUpdateCheckDone` 保证本次运行只查一次。关于页手动「检查更新」不变。  
**为什么**：用户希望有网时主动提示升级。  
**涉及文件**：`electron/renderer/app.js`。  
**改后效果**：刷新 renderer 或重启易知后生效；须配置 `MYKNOWLEDGE_UPDATE_URL`（安装包一般已注入）。  
**发版**：否，随下一安装包；开发态热加载即可验。

---

#### 2026-08-13 11:09 — 发布 3.0.0（服务闭环大版本）· 已构建

**改了什么**：版本升至 3.0.0；同步 changelog、latest.json、安装向导亮点/欢迎语/进度轮播、闲鱼下载链、新手指引 v6。本版卖点：服务闭环、五场景模板、到期资产、知识库初始化。  
**为什么改**：用户确认重大更新发主版本。  
**涉及文件**：`electron/package.json`、`package-lock.json`、`YizhiSetup.iss`、`installer-highlights.txt`、`changelog.html`、`latest.json`、闲鱼手册、`onboarding.js`。  
**改后效果**：本地产物 `dist\Yizhi-Setup-3.0.0.exe`（约 439 MB）；须人工压成 `Yizhi-Setup-3.0.0.zip` 后上传 `yizhi/`：zip、`latest.json`、`changelog.html`。  
**发版**：是 → 待上传服务器三件套。

---

#### 2026-08-13 10:54 — 修复「测试连接」PATCH 未带 JSON Content-Type

**做了什么**：`service-loop.js` 请求封装统一补 `Content-Type: application/json` 并支持 object body，避免测通成功后标记 `key_ok` 时 FastAPI 把 body 当成字符串报 `model_attributes_type`。  
**涉及文件**：`electron/renderer/service-loop.js`、`app.js`。  
**发版**：否。

---

#### 2026-08-13 10:36 — 订阅服务闭环 P0/P1/P2（教师六步·OPC·职场）· 已测试

**做了什么**：按规格落地试用转化闭环——P0 大模型「测试连接」+ 顶栏四步任务条；P1 试用四周剧本 + 到期/剩余≤7 天授权页「本地知识资产」；P2 五场景模板（教师备课→上课→练习→作业→检测→反馈、考研/教研、自媒体、职场、一人公司 OPC），草案预览确认后写入笔记。进度存 `.config/service-loop.json`，初始化清零时一并删除。  
**为什么**：头条「先别卖功能，要设计服务闭环」；强化垂直场景可交付物。  
**涉及文件**：`lib/service_loop.py`、`lib/llm.py`（probe）、`backend/server.py`、`electron/renderer/service-loop.js`、`app.js`、`index.html`、`styles.css`、`wiki_reset.py`、`tests/test_service_loop.py`、规格与闲鱼手册。  
**改后效果**：`pytest tests/test_service_loop.py` 6 passed。  
**发版**：否，随下一安装包。

---

#### 2026-08-12 16:40 — 设置「初始化」：清空知识库（强提醒）· 已测试

**做了什么**：设置新增选项卡「初始化」；预览将删内容与免责；勾选确认 + 输入「确认清空知识库」+ 二次 confirm 后清零笔记/导入/索引/记忆/资料夹与自定义工作流配置/外联登记等；外联只清登记与 `MYKNOWLEDGE_EXTRA_DIRS`，不删外部真实目录与文件；保留 .env 中 API Key/授权；重建空骨架与默认资料夹。  
**为什么**：用户需要可谨慎重置知识库的能力。  
**涉及文件**：`lib/wiki_reset.py`、`backend/server.py`（`/api/wiki/reset*`）、`electron/renderer/app.js`、`styles.css`、`tests/test_wiki_reset.py`。  
**改后效果**：`pytest tests/test_wiki_reset.py` 3 passed。  
**发版**：否，随下一安装包。

---

#### 2026-08-12 15:11 — Mnemosyne 长记忆评估：不整装 + 事实版本链规格

**做了什么**：评估[头条 Mnemosyne 改版](https://www.toutiao.com/article/7672859422640603700/)；结论不装进易知；写入评估总表与三乙附节；新增规格 `docs/superpowers/specs/2026-08-12-memory-fact-versions-design.md`（在现有 `memory.db` 上做 facts 版本链 MVP，默认关，待痛点再实现）。  
**为什么**：吸收版本/时序/隔离原则，避免叠第三套记忆引擎。  
**涉及文件**：`Cursor-头条Skills评估与使用说明.md`、上述 specs。  
**改后效果**：后续实现有验收标准；产品线仍以 wiki + memory_db + distill 为主。  
**发版**：否。

---

#### 2026-08-13 16:10 — 头条「去 AI 味 10 Skills」评估与三处落地 · 已测试

**做了什么**：评估[头条文](https://www.toutiao.com/article/7673332939782029858/)后**不全装**；全局安装 `humanizer-zh`、`shuorenhua`、`de-ai-writing`；「我读史记 / Articles」规则默认叠加；易知写文章注入增强包（`MYKNOWLEDGE_DE_AI_WRITING`，默认开）。  
**为什么改**　十套重叠会打架；互补三项覆盖中文痕迹、说人话、生成硬门槛。  
**涉及文件**：`.cursor/rules/wodu-shiji*`、`articles-hotspot*`、`Articles/00-写作规则.md`、`lib/de_ai_writing.py`、`lib/prompts.py`、`lib/settings.py`、`skills/de-ai-writing/`、`tests/test_de_ai_writing.py`、评估说明、vendor 三仓。  
**改后效果**：`pytest tests/test_de_ai_writing.py`；写稿无需点名即叠三 Skill；易知设置可关增强包。  
**发版**：否，随下一安装包。

---

**做了什么**：①「我读史记 / Articles」规则与人类可读文档加「思想群岛 5～8 条」硬步骤与中间成果包说明；②易知 `skills/basb-writing/` + `lib/basb_writing.py`，写文章 system 注入，蒸馏 Meta/SYSTEM/层 README 对齐 BASB，设置 `MYKNOWLEDGE_BASB`（默认开）；③Cursor 全局显式 `basb-writing`。来源[头条 Tiago/BASB](https://www.toutiao.com/article/7672440685642990114/)。  
**为什么改**　按评估建议依次完成，避免空白开写、强化复用。  
**涉及文件**：两套 `.mdc`、史记/Articles `00-写作规则`、`lib/basb_writing.py`、`lib/prompts.py`、`lib/distill.py`、`lib/settings.py`、`prompts/produce.md`、`skills/basb-writing/`、`tests/test_basb_writing.py`、`~/.cursor/skills/basb-writing/`、评估说明。  
**改后效果**：`pytest tests/test_basb_writing.py tests/test_dzs_framework.py` 通过。  
**发版**：否，随下一安装包。

---

#### 2026-08-11 19:20 — 我读史记 / Articles 热点默认叠加 DZS

**做了什么**：`wodu-shiji-wechat-writing.mdc`、`articles-hotspot-writing.mdc` 及人类可读规则写入 `dzs-prompt-framework`；闭环在材料关后静默催化；全局 DZS Skill 增加「公众号默认叠加」说明；易知「公众号热点稿」体例点名 DZS。  
**为什么改**　用户要求两套写稿默认技能含 DZS。  
**涉及文件**：`.cursor/rules/wodu-shiji-wechat-writing.mdc`、`.cursor/rules/articles-hotspot-writing.mdc`、`Articles/00-写作规则.md`、`D:\工作文档\史记\00-我读史记写作规则.md`、`~/.cursor/skills/dzs-prompt-framework/SKILL.md`、`lib/produce_genres.py`、`Cursor-头条Skills评估与使用说明.md`。  
**改后效果**：写「我读史记」或 Articles 热点时 Agent 须 Read 并静默跑 DZS，无需用户点名。  
**发版**：否（规则/Skill；易知体例随下一包）。

---

#### 2026-08-11 19:15 — 提问/写文章/推演内置 DZS 认知催化 · 已测试

**做了什么**：内置 `skills/dzs-prompt-framework/`（`yizhi-rules.md`）；`lib/dzs_framework.py`；`system_for_ask` / `system_for_produce` / `system_for_deduce` 默认注入；设置项「DZS 认知催化」（`MYKNOWLEDGE_DZS`，默认开）。适配单次流式：静默五阶段与压力测试，禁止输出脉令 JSON、禁止打断等意图确认。  
**为什么改**　用户要求易知文章/分析/报告类产出运行 DZS；与 Cursor 须显式 Skill 分流（编码会话仍须点名）。  
**涉及文件**：`lib/dzs_framework.py`、`lib/prompts.py`、`lib/settings.py`、`skills/dzs-prompt-framework/`、`tests/test_dzs_framework.py`、`.env.example`、`Cursor-头条Skills评估与使用说明.md`。  
**改后效果**：`pytest tests/test_dzs_framework.py` 通过；关设置后三端 system 不再含 DZS。  
**发版**：否，随下一安装包。

---

#### 2026-08-11 07:19 — 头条 ParseBench 思路本地化：LLM 版面解析

**做了什么**：评估[头条文](https://www.toutiao.com/article/7672221541857378850/)后落地：`lib/pdf_llm_parse.py`（表格 HTML+colspan/rowspan、bbox/label div、跨页合表提示）；工作流「LLM 版面解析」写 `notes/parse-preview/`；RAG 对 HTML 表按 `<tr>` 切分；导入可选 `MYKNOWLEDGE_PDF_LLM_PARSE=1`（默认关）。不捆绑 gaik/LlamaParse。  
**为什么**：复杂采购单类版面，本地解析器易错位；用对多模态输出契约比换解析器更快见效。  
**涉及文件**：`lib/pdf_llm_parse.py`、`lib/extract.py`、`lib/rag_chunk.py`、`lib/skills.py`、`lib/workflows.py`、`lib/capabilities.py`、`skills/wiki-pdf-llm-parse/`、`tests/test_pdf_llm_parse.py`、`Cursor-头条Skills评估与使用说明.md`。  
**改后效果**：`pytest tests/test_pdf_llm_parse.py tests/test_rag_chunk_tables.py` 通过；能力面板可见「LLM 版面解析」。  
**发版**：否，随下一安装包；需多模态 API。

---

#### 2026-08-11 06:27 — 重打 2.5.1 安装包（含新安装向导文案）

**做了什么**：按更新后的欢迎页 / 亮点页 / 进度轮播文案重跑构建；产物覆盖 `dist/Yizhi-Setup-2.5.1.exe`（`EXIT=0`，约 439MB，时间戳 2026-08-11 06:26）。  
**为什么**：此前 2.5.1 包打在文案更新之前，用户安装时看不到本版特点。  
**涉及文件**：`dist/Yizhi-Setup-2.5.1.exe`、staging `Yizhi-Staging-2.5.1-20260811055830`、`latest.json`（构建后已恢复完整 release_notes）。  
**改后效果**：安装向导展示 2.5.1 自我蒸馏 / PDF / Win64 等亮点。  
**发版**：是 → 人工压 `Yizhi-Setup-2.5.1.zip` 后上传。

---

#### 2026-08-11 05:56 — 发版必改安装向导文案（流程固化）

**做了什么**：明确以后每次发版须同步安装界面文案——`installer-highlights.txt`（亮点页）、`YizhiSetup.iss` 的欢迎页与进度轮播 `GetInstallQuote`；已按 2.5.1 卖点改写上述文案；`发版操作手册` §2.4、验收表、`.cursor/rules/yizhi-update-log.mdc` 写入强制检查项。  
**为什么**：用户安装过程中应看到本版特点，不能仍是上版通用介绍。  
**涉及文件**：`scripts/installer/installer-highlights.txt`、`scripts/installer/YizhiSetup.iss`、`docs/commercial/发版操作手册.md`、`docs/commercial/构建安装包.md`、`.cursor/rules/yizhi-update-log.mdc`。  
**改后效果**：发版清单含安装向导文案；源码侧 2.5.1 文案已就绪。  
**发版**：文案已进仓库；**已打好的** `Yizhi-Setup-2.5.1.exe` 仍是旧向导文案，若要对用户展示新亮点须**重打安装包**后再压 zip 上传。

---

#### 2026-08-10 23:15 — 全新发版 2.5.1（P2 候选项）

**做了什么**：正式发版 **2.5.1**。落地 P2：蒸馏预览确认入库 + 提问页 Skills/Principle 可视化；DocuBrowser 测试/文档 prune；FAQ/新手指引 v5（Win64）；PDF 分栏阅读序 + 可选视觉图注（默认关）。含既有表行安全分块与解析质量抽检。  
**为什么**：把 2.5.0 后的体验与解析缺口一次打磨进安装包。  
**涉及文件**：`lib/distill.py`、`lib/extract.py`、`lib/llm.py`、`electron/renderer/app.js`、`onboarding.js`、`scripts/prune_install_staging.ps1`、`docs/commercial/*`、版本号与安装包产物。  
**验后效果**：版本 2.5.1；产物 `dist/Yizhi-Setup-2.5.1.exe`；对外下载链 `.zip`。  
**备注**：人工将 exe 压成 `Yizhi-Setup-2.5.1.zip` 后与 `latest.json`、`changelog.html`、`faq.html` 一并上传 `yizhi/`。

---

#### 2026-08-10 21:05 — 解析质量 A：表行安全分块 + 抽检工作流

**做了什么**：RAG 分块对 Markdown 表按行切分（不切断表行）；新增工作流「解析质量抽检」（`wiki-parse-check` / `lib/parse_quality.py`）；能力面板增加 parse_quality。不内置 MinerU。  
**为什么**：落地头条「文档解析」文章中与易知缺口对齐的轻量方案 A。  
**涉及文件**：`lib/rag_chunk.py`、`lib/parse_quality.py`、`lib/skills.py`、`lib/workflows.py`、`lib/capabilities.py`、`skills/wiki-parse-check/`、`tests/test_rag_chunk_tables.py`、`tests/test_parse_quality.py`。  
**验后效果**：`pytest tests/test_rag_chunk_tables.py tests/test_parse_quality.py` 通过。  
**发版**：否（随下一安装包）。

---

#### 2026-08-09 12:41 — 全新发版 2.5.0（自我蒸馏）

**做了什么**：正式发版 **2.5.0**。相对 2.4.2 的核心新增是**自我蒸馏**：本地 `distill/` 四层（Memory / Skills / Principle / Meta）；工作流「蒸馏本次产出」；提问与写文章自动注入 Skills+Principle；新手指引 v4。继承 2.4.2 的 PDF 整表 Markdown 与扫描表 ONNX/OCR。  
**为什么**：把「经验复利」做成每个用户开箱可用的本地闭环，作为 2.5 主叙事。  
**涉及文件**：`electron/package.json`、`scripts/installer/YizhiSetup.iss`、`docs/commercial/changelog.html`、`docs/commercial/yizhi-web/latest.json`、`lib/distill.py`、`skills/wiki-distill/`、`backend/server.py`、安装包产物。  
**验后效果**：版本 2.5.0；产物 `dist/Yizhi-Setup-2.5.0.exe`；对外下载链 `.zip`。  
**备注**：人工将 exe 压成 `Yizhi-Setup-2.5.0.zip` 后与 `latest.json`、`changelog.html` 一并上传 `yizhi/`。

---

#### 2026-08-09 10:35 — 自我蒸馏（骨架 + 工作流 + 注入）

**做了什么**：wiki 下幂等创建 `distill/{memory,skills,principle,meta}`；工作流「蒸馏本次产出」（`wiki-distill`）；提问/写文章注入 Skills+Principle；新手指引升 v4。  
**为什么**：落地「AI 蒸馏自己」方法论 1+2，人人本地可复用，不绑行业模板。  
**涉及文件**：`lib/distill.py`、`lib/config.py`、`lib/skills.py`、`lib/workflows.py`、`lib/llm.py`、`skills/wiki-distill/`、`electron/renderer/onboarding.js`、`backend/server.py`。  
**验后效果**：`pytest tests/test_distill.py`；启动后见 distill 目录；工作流可跑蒸馏。  
**发版**：是 → 随 2.5.0 安装包。

---

#### 2026-08-07 21:40 — 全新发版 2.4.2

**做了什么**：正式发版 2.4.2。相对 2.4.1：PDF 数字表格整表 Markdown 入库；扫描/弱文本页 ONNX 表检（随包 `third_party/pdf_layout`）+ Tesseract/启发式降级；能力面板增加 pdf_tables / pdf_layout。不内置 Crawl4AI。  
**为什么**：用户确认按建议发版；结构化 PDF 已落地需进安装包。  
**涉及文件**：`electron/package.json`、`lib/pdf_tables.py`、`lib/pdf_layout.py`、`lib/extract.py`、`third_party/pdf_layout/`、`docs/commercial/*`。  
**验后效果**：版本 2.4.2；产物 `dist/Yizhi-Setup-2.4.2.exe`；对外下载链 `.zip`。  
**备注**：旧 PDF 需重新导入；扫描表需本机 Tesseract（建议 chi_sim）；人工将 exe 压 zip 后上传。

---

#### 2026-08-07 16:22 — PDF 结构化表格（B+C）· 已测试

**做了什么**：数字 PDF 用 pdfplumber 整表转 Markdown；弱文本页可选 ONNX 表检 + Tesseract 网格/启发式；随包目录 `third_party/pdf_layout`；开关 `MYKNOWLEDGE_PDF_TABLES` / `MYKNOWLEDGE_PDF_LAYOUT`。  
**为什么**：借鉴 RAGFlow DeepDoc「表格不拆碎」思路，但不依赖 Docker，安装包可换机用。  
**涉及文件**：`lib/pdf_tables.py`、`lib/pdf_layout.py`、`lib/extract.py`、`lib/capabilities.py`、`requirements-runtime.txt`、`third_party/pdf_layout/`、`scripts/fetch_pdf_layout_models.ps1`。  
**验后效果**：`pytest tests/test_pdf_tables.py tests/test_pdf_layout_fallback.py`；导入含表 PDF 可见 `第N页-表格K`。  
**发版**：否，随下一安装包；开发机可先跑 `scripts/fetch_pdf_layout_models.ps1` 拉取 detection.onnx。

---

#### 2026-08-05 17:06 — 全新发版 2.4.1

**做了什么**：正式发版 2.4.1。相对 2.4.0：PDF 内嵌图/扫描页 OCR 进 RAG（Tesseract+PyMuPDF）；左上角订阅角标仅付费订阅显示（试用/未授权隐藏）；继承 2.4.0 安装启动、内置 ffmpeg、播客完整合成、写文章流式布局、反馈弹窗、新手指引 v3 等修复。  
**为什么**：用户要求全新发版 2.4.1。  
**涉及文件**：lectron/package.json、docs/commercial/changelog.html、docs/commercial/yizhi-web/latest.json、docs/更新记录.md、安装包产物。  
**验后效果**：版本号 2.4.1；Setup 产物 dist/Yizhi-Setup-2.4.1.exe。  
**备注**：旧 PDF 需重新导入才补 OCR；用户机需安装 Tesseract（含 chi_sim）。

---

#### 2026-08-05 14:45 — 写文章内化 human-writing + 公众号体例 · 已测试

**改了什么**　新增 `skills/human-writing/`（`produce-rules.md` + `SKILL.md`）与 `lib/human_writing.py`；`system_for_produce()` 默认注入活人感材料关/硬禁辞章（`MYKNOWLEDGE_HUMAN_WRITING`，设置页可关）；`produce_genres` 增加「公众号热点稿」`wechat`（2000–5000 字）。与既有 no-ai-slop 并列，随 `skills/` 进安装包，不依赖 Cursor。  
**为什么改**　用户要求 Articles 写作技能内化到易知「写文章」，保证安装到其他电脑仍生效。  
**涉及文件**　`lib/human_writing.py`、`lib/prompts.py`、`lib/settings.py`、`lib/produce_genres.py`、`skills/human-writing/*`、`skills/README.md`、`.env.example`、`docs/更新记录.md`  
**改后效果**　写文章/改稿默认叠加 human-writing + no-ai-slop；体例可选「公众号热点稿」。本地 `python -c` 注入与体例冒烟通过。  
**发版**　否，随下一版 Setup（无需额外依赖）

---

#### 2026-08-05 10:56 — PDF 内嵌图/扫描页 OCR 进 RAG（Tesseract）· 已测试

**改了什么**　`_extract_pdf`：文本层保留；弱文本页整页光栅化 OCR；富文本页抽内嵌大图 OCR；结果写入提取文本自动进分块/FTS/向量。依赖 `pymupdf`；OCR 复用 Tesseract `chi_sim+eng`。  
**为什么改**　扫描 PDF / 图文混排此前只索引文本层或「无文本层」占位，图中文字无法检索。  
**涉及文件**　`lib/extract.py`、`lib/capabilities.py`、`requirements-runtime.txt`、`scripts/verify_pdf_ocr.py`、`docs/更新记录.md`  
**改后效果**　导入 PDF 后 `.extracted` 可含「图M OCR / 扫描 OCR」；`MYKNOWLEDGE_PDF_OCR=0` 可关；需本机 Tesseract + pymupdf。  
**发版**　否，随下一版 Setup（须打包 pymupdf；用户机仍需 Tesseract）

---

#### 2026-08-04 13:43 — 重发 2.4.0（订阅角标 + 既有修复）· 已测试

**改了什么**　重打 `Yizhi-Setup-2.4.0.exe`（约 325.7 MB，staging `20260804131524`）；`changelog.html` 补「订阅状态展示」、日期 2026-08-04；同步 `latest.json`。  
**为什么改**　试用角标隐藏等热修须进入对外安装包。  
**涉及文件**　`dist/Yizhi-Setup-2.4.0.exe`、`docs/commercial/changelog.html`、`docs/commercial/yizhi-web/latest.json`、`docs/更新记录.md`  
**改后效果**　新装即含左上角仅付费显示订阅状态，以及此前播客/布局/指引/反馈修复。  
**发版**　是 → 上传 `yizhi/`：`Yizhi-Setup-2.4.0.exe`、`latest.json`、`changelog.html`

---

#### 2026-08-04 13:11 — 左上角订阅状态：试用不再显示 · 已测试

**改了什么**　`licenseBannerText`：试用 / 未授权时隐藏 `#license-banner`；仅付费订阅（含永久）显示状态。  
**为什么改**　试用信息打扰；左上角只应标示付费订阅身份。  
**涉及文件**　`electron/renderer/app.js`、`docs/更新记录.md`  
**改后效果**　试用用户顶栏无「试用剩余 N 天」；订阅用户仍显示方案与到期日。关于页试用说明保留。  
**发版**　热修 renderer；随下一版 Setup

---

#### 2026-08-03 22:13 — 重发 2.4.0（指引 v3 + 反馈弹窗 + changelog）· 已测试

**改了什么**　重打 `Yizhi-Setup-2.4.0.exe`（约 325.7 MB，staging `20260803214047`：含指引 v3、反馈弹窗加大、播客/布局修复、ffmpeg）；`changelog.html` 已写主要更新并统一 `.exe` 下载链。  
**为什么改**　热修内容须进入对外安装包；用户向更新记录须反映本轮主要修复。  
**涉及文件**　`dist/Yizhi-Setup-2.4.0.exe`、`docs/commercial/changelog.html`、`docs/commercial/yizhi-web/latest.json`、`docs/更新记录.md`  
**改后效果**　新装即含指引 v3 / 反馈可用编辑区 / 播客与布局修复。  
**发版**　是 → 上传 `yizhi/`：`Yizhi-Setup-2.4.0.exe`、`latest.json`、`changelog.html`

---

#### 2026-08-03 21:36 — 反馈弹窗：正文编辑区被挤没 · 已测试

**改了什么**　`.modal-card.feedback-modal-card` 双类覆盖 `max-width:420px`；弹窗约 880×640；编辑区 `min-height:280px`；挂载后按宿主高度 `setHeight`。  
**为什么改**　后面的 `.modal-card{max-width:420px}` 盖掉加宽规则，4:3 矮窗只剩工具栏一条缝。  
**涉及文件**　`electron/renderer/styles.css`、`app.js`、`docs/更新记录.md`  
**改后效果**　反馈弹窗明显变大，正文区可正常多行编辑。  
**发版**　热修 renderer；随下一版 Setup

---

#### 2026-08-03 18:02 — 新手指引 v3（对齐 2.4.0）· 已测试

**改了什么**　`onboarding.js` 升至 `myk-onboarding-v3`：补推演、记忆与进化/矛盾检测、续航与知签、写文章改稿·定稿·播客、B 站字幕/外联、资料夹、反馈等说明。  
**为什么改**　2.4.0 功能面扩大，旧 v2 指引仍停在导入→笔记→提问，易漏核心能力。  
**涉及文件**　`electron/renderer/onboarding.js`、`docs/更新记录.md`  
**改后效果**　新装或未完成 v3 的用户自动走新路线；已完成 v2 者升级后会再看一遍；关于→新手指引可重播。  
**发版**　热修 renderer；随下一版 Setup 打包

---

#### 2026-08-02 22:13 — 重发 2.4.0（播客完整合成 + 写文章流式布局）· 已测试

**改了什么**　重打 `Yizhi-Setup-2.4.0.exe`（约 325.7 MB，staging `20260802214945`：ffmpeg 8 DLL、播客解析修复、写文章 auto-height）；同步 `changelog.html`（补播客/布局要点，下载链改为 `.exe`）。  
**为什么改**　对外包须包含本机已验证的启动、播客截断与布局修复，不能只靠安装目录热修。  
**涉及文件**　`dist/Yizhi-Setup-2.4.0.exe`、`docs/commercial/changelog.html`、`docs/commercial/yizhi-web/latest.json`、`docs/更新记录.md`  
**改后效果**　新装用户开箱即可完整播客合成与正常写文章布局。  
**发版**　是 → 上传 `yizhi/`：`Yizhi-Setup-2.4.0.exe`、`latest.json`、`changelog.html`

---

#### 2026-08-02 21:05 — 播客音频误截断：对话含「脚注」等词即停 · 已测试

**改了什么**　`extract_podcast_dialogue_text` 停止条件改为「整行是章节标题」匹配，不再对台词做「脚注/摘要/附录…」子串截断。  
**为什么改**　稿中嘉宾说「脚注标明来源」时 TTS 只合成前 5 句（约 1 分钟），后文整段丢失。  
**涉及文件**　`lib/podcast.py`、`docs/更新记录.md`  
**改后效果**　同篇稿解析为 20 句对话（全文）；重新点「合成播客音频」可得完整 `full.wav`。  
**发版**　已热修 `C:\Program Files\Yizhi\lib\podcast.py`；须重启后端；随 2.4.0 重打包

---

#### 2026-08-02 20:55 — 写文章改为整页滚动 · 已测试

**改了什么**　`#panel-produce` 统一 `overflow-y:auto`；去掉播客坞/确认区/输出区各自限高滚动；编辑器 `growWithContent` 随正文增高。  
**为什么改**　多区域小滚动条不科学，音频/确认内容被挤进细条。  
**涉及文件**　`electron/renderer/styles.css`、`rich-editor.js`、`app.js`、`docs/更新记录.md`  
**改后效果**　本页一条滚动条看完全部控件与正文。  
**发版**　热修 renderer

---

#### 2026-08-02 20:45 — 写文章面板溢出：上方控件被挤出 · 已测试

**改了什么**　确认面板/播客坞限高内滚；待确认改为可折叠；编辑器改为 flex 填满剩余高度（取消固定 420px）；上方主题/工具栏/第一性等 `flex-shrink:0`。  
**为什么改**　确认块+高编辑器把输入框、合成/下载等顶出 `overflow:hidden` 视口。  
**涉及文件**　`electron/renderer/styles.css`、`app.js`、`docs/更新记录.md`  
**改后效果**　主题输入、第一性、确认定稿、音频播放与下载保持可见。  
**发版**　热修 renderer

---

#### 2026-08-02 20:35 — 写文章：播客播放/下载坞 + 待确认可勾选 · 已测试

**改了什么**　播客完整音频固定展示在工具栏下「播客坞」（播放器 + 下载）；「待人工确认」从 Markdown 解析到独立面板，可逐项勾选 / 全部确认（正文在富文本编辑器内时原先两者都找不到）。  
**为什么改**　状态写「已就绪」但页面无播放/下载；待确认表无操作入口。  
**涉及文件**　`electron/renderer/index.html`、`app.js`、`styles.css`、`docs/更新记录.md`  
**改后效果**　合成后立即可见播放与下载；待确认项可勾选。  
**发版**　热修 `electron/renderer`；正式分发并入 Setup

---

#### 2026-08-02 17:20 — 安装包强制打包可运行 ffmpeg · 已测试

**改了什么**　`build-portable -ForInstaller` 校验 `third_party/ffmpeg/bin`（exe + ≥5 DLL + `-version`）；缺则从开发树补拷或构建失败；`setup_optional_tools` 默认允许拉取官方 ffmpeg（`MYKNOWLEDGE_BUNDLE_FFMPEG=1`）。  
**为什么改**　用户机播客合并依赖 ffmpeg，不能只靠「可选」。  
**涉及文件**　`scripts/build-portable.ps1`、`scripts/setup_optional_tools.py`、`docs/更新记录.md`  
**改后效果**　打不出缺 ffmpeg/DLL 的 Setup；已有完整 bin 的树可直接重打安装包带走。  
**发版**　须重打 Setup 后覆盖上传

---

#### 2026-08-02 17:15 — 播客完整音频：同格式合并 + 在线播放/下载 · 已测试

**改了什么**　全 WAV（智谱等）默认拼 `full.wav`，不再因有 ffmpeg 就强转 mp3；ffmpeg 失败回退 WAV；前端优先展示完整播放器与「下载完整音频」。  
**为什么改**　界面只显示「共 N 段」、无完整文件可播可下。  
**涉及文件**　`lib/podcast.py`、`lib/podcast_merge.py`、`lib/runtime_tools.py`、`electron/renderer/app.js`、`docs/更新记录.md`  
**改后效果**　合成后得到单一完整文件，可在线播放与下载；本机已热修 Program Files 的 runtime + `app.js`。  
**发版**　热修；正式分发并入下一 Setup

---

#### 2026-08-02 17:10 — 播客合成 ffmpeg 黑窗一闪 · 已测试

**改了什么**　`subprocess_run_hidden`（`CREATE_NO_WINDOW`）合并播客；ffmpeg 以 bin 为 cwd；前端在完整合并失败时显示 `merged_error` 而非笼统「合成完成」。  
**为什么改**　安装版调用控制台版 ffmpeg 会闪黑窗；合并失败时界面仍像成功。  
**涉及文件**　`lib/runtime_tools.py`、`lib/podcast_merge.py`、`electron/renderer/app.js`、`docs/更新记录.md`  
**改后效果**　合并不再弹控制台；失败原因可见。安装版须更新 runtime + `app.js`（或重打 Setup）。  
**发版**　热修；正式分发建议并入下一 Setup

---

#### 2026-08-02 16:36 — 重发 2.4.0（含安装启动修复）· 已测试

**改了什么**　用修复后的冻结后端重打 `Yizhi-Setup-2.4.0.exe`（约 326 MB）；`latest.json` 发布说明补上启动修复。  
**为什么改**　首发 Setup 的 runtime 仍会 DocuBrowser 旁路风暴 / `None.isatty` 崩溃，需覆盖上传。  
**涉及文件**　`dist\Yizhi-Setup-2.4.0.exe`、`docs\commercial\yizhi-web\latest.json`、`docs\commercial\changelog.html`、`docs\更新记录.md`  
**改后效果**　安装包内 runtime 含 sidecar + stdio/日志修复；staging 无控制台 `/api/health` 已通。  
**发版**　是 → 覆盖上传 `yizhi/`：`Yizhi-Setup-2.4.0.exe`、`latest.json`、`changelog.html`

---

#### 2026-08-02 15:40 — 安装版后端启动卡死：冻结 exe 误跑 DocuBrowser · 已测试

**改了什么**　冻结入口识别 `yizhi-backend.exe script.py …` 并 `run_path` 执行旁路；旁路前把脚本目录插入 `sys.path`；模块加载即补 stdio；冻结/无控制台用 uvicorn Formatter 且字典内 `use_colors: False`（杜绝 `None.isatty`）；DocuBrowser 引导改后台线程。  
**为什么改**　安装版把 `sys.executable`（冻结 exe）当 Python 启 DocuBrowser，却再开一套 FastAPI，startup 风暴导致 18765 超时。  
**涉及文件**　`backend/__frozen_main__.py`、`backend/server.py`、`docs/更新记录.md`、`docs/commercial/changelog.html`  
**改后效果**　本机已覆盖 `C:\Program Files\Yizhi\runtime`；主引擎 `/api/health` 与 DocuBrowser 18766 均可通。正式分发须重打 Setup 或提供 runtime 热修包。  
**发版**　热修 runtime（2.4.0）；建议重编 `Yizhi-Setup-2.4.0.exe` 覆盖上传

---

#### 2026-08-02 14:26 — 发布 2.4.0 · 已测试

**改了什么**　版本升至 **2.4.0**；同步 `changelog.html` / `latest.json` / Inno / API 版本；汇总记忆与进化、矛盾检测、检索增强、今日知签、leader、去 AI 味、反馈与安装包瘦身等用户可见能力。  
**为什么改**　2.3.0 之后功能合集对外发版。  
**涉及文件**　`electron/package.json`、`electron/package-lock.json`、`scripts/installer/YizhiSetup.iss`、`backend/server.py`、`docs/commercial/changelog.html`、`docs/commercial/yizhi-web/latest.json`、`docs/更新记录.md`  
**改后效果**　检查更新指向 2.4.0；关于→更新记录含本版说明；`dist\Yizhi-Setup-2.4.0.exe` 约 326 MB。  
**发版**　是 → `dist\Yizhi-Setup-2.4.0.exe`；须上传 `yizhi/`：exe、`latest.json`、`changelog.html`

---

#### 2026-08-10 20:05 — LoopX 长程控制平面接入 Cursor 全局 · 已测试

**改了什么**　评估[头条文](https://www.toutiao.com/article/7672039193704825386/)后安装 [LoopX](https://github.com/huangruiteng/loopx)：`pip install -e`、`slash-commands --install --surface cursor`、复制 `loopx*` Skills、MCP 写入 `mcp.json`；规则 `~/.cursor/rules/loopx.mdc`。  
**为什么改**　提升 Cursor 跨天/跨会话长任务治理（目标、门禁、配额、证据），官方支持 Cursor。  
**涉及文件**　`e:\app\vendor\loopx\`、`%USERPROFILE%\.cursor\skills\loopx*`、`mcp.json`、`loopx.mdc`、评估说明  
**改后效果**　`loopx doctor` ok；业务仓可 `loopx connect` / `start-goal`；Agent 可调用 loopx Skill。  
**发版**　否（Cursor/本机工具链）

---

#### 2026-08-05 11:35 — 活人感写作 human-writing 全局化并接入我读史记 · 已测试

**改了什么**　安装 [human-writing](https://github.com/KKKKhazix/human-writing) 到全局 Skills；新增 `~/.cursor/rules/human-writing.mdc`；「我读史记」`.mdc` / `00-风格积累` / `00-我读史记写作规则` 闭环加入材料关与硬禁自检。  
**为什么改**　按[头条文](https://www.toutiao.com/article/7670371926468084234/)评估可用，补义理·考据，不只去辞章 AI 皮。  
**涉及文件**　`%USERPROFILE%\.cursor\skills\human-writing\`、`human-writing.mdc`、`wodu-shiji-wechat-writing.mdc`、`D:\工作文档\史记\00-*.md`、评估说明  
**改后效果**　一般写作可点名 human-writing；写「我读史记」自动叠加材料关 + 硬禁 + no-ai-slop。  
**发版**　否

---

#### 2026-07-30 15:50 — last30days 纳入头条/微博/彭博站点源 · 已测试

**改了什么**　在 last30days 引擎新增默认开启的 `toutiao`/`weibo`/`bloomberg`（`site:` + web/keyless）；说明见 `LOCAL_EXTENSIONS.md`。  
**为什么改**　选题调研需覆盖中文站与彭博新闻，又不做需登录的原生爬虫。  
**涉及文件**　`vendor/last30days-skill/skills/last30days/scripts/lib/site_sources.py` 及 pipeline/normalize/render/ui/signals/planner；全局 `%USERPROFILE%\.cursor\skills\last30days\`；`Cursor-头条Skills评估与使用说明.md`  
**改后效果**　`--search bloomberg --mock` 可跑通；`EXCLUDE_SOURCES` 可关三源。  
**发版**　否（Cursor Skill 本机扩展）

---

#### 2026-07-30 15:40 — Cursor 全局安装 last30days Skill · 已测试

**改了什么**　按[头条文](https://www.toutiao.com/w/1871788301735946/)安装 [last30days](https://github.com/mvanhorn/last30days-skill) 到 `%USERPROFILE%\.cursor\skills\last30days\`；vendor + 使用说明写入 `Cursor-头条Skills评估与使用说明.md`。  
**为什么改**　选题/近 30 天社区讨论调研，与既有头条 Skill 同流程全局本地化。  
**涉及文件**　`Cursor-头条Skills评估与使用说明.md`、`docs/更新记录.md`（引擎在用户全局 skills）  
**改后效果**　Cursor 可 `使用 last30days：话题`；`--help` 冒烟通过；Reddit/HN 等零配置可用。  
**发版**　否（Cursor 配置，非易知安装包）

---

#### 2026-07-30 15:30 — 写文章自动去 AI 味（no-ai-slop）· 已测试

**改了什么**　内置 `skills/no-ai-slop/`；`system_for_produce` 默认注入 `produce-rules.md`；设置项「写文章去 AI 味」（`MYKNOWLEDGE_NO_AI_SLOP`，默认开）。  
**为什么改**　最终用户在 GUI「写文章」无需点选即可少套话，与 Cursor 全局 Skill 同源落地。  
**涉及文件**　`lib/no_ai_slop.py`、`lib/prompts.py`、`lib/settings.py`、`skills/no-ai-slop/`、`.cursor/skills/no-ai-slop/`、`skills/README.md`、`docs/更新记录.md`  
**改后效果**　写文章/改稿 system 含禁用词与禁用模式；设置可关；`yws skill show/match no-ai-slop` 可用。  
**发版**　否（随后续安装包；须重启后端）

---

#### 2026-07-30 15:20 — Cursor 头条记忆篇 Skills 评估落盘 · 已测试

**改了什么**　评估 [MiMo/Memora/Flow2Spec 文](https://www.toutiao.com/article/7662547329391526419/)：全局装 `f2s-*`×20 + `agent-session-memory`；说明写入 `Cursor-头条Skills评估与使用说明.md`。  
**为什么改**　与 no-ai-slop 等同流程：评估→本地化→使用说明。  
**涉及文件**　`Cursor-头条Skills评估与使用说明.md`、`docs/更新记录.md`（Skills 在 `%USERPROFILE%\.cursor\skills\`）  
**改后效果**　可立刻用轻量记忆；Flow2Spec 须在业务仓 `npx … flow2spec init`。  
**发版**　否（Cursor 配置与文档，非易知安装包）

---

#### 2026-07-30 15:35 — 反馈弹窗表单不再横向溢出 · 已测试

**改了什么**　反馈卡与编辑器宿主加 `min-width:0` / `max-width:100%`，约束 Toast UI 工具栏与正文宽度。  
**为什么改**　富文本区比上方输入框更宽，挤出弹窗右缘。  
**涉及文件**　`electron/renderer/styles.css`、`docs/更新记录.md`  
**发版**　否（刷新 renderer）

---

#### 2026-07-30 15:32 — 「今日知签」列表左对齐 · 已测试

**改了什么**　`#fortune-modal` / `.fortune-panel` 及内部 `ul/li` 强制左对齐并规范列表缩进。  
**为什么改**　知签宜忌、库运势列表在弹窗里仍呈居中，圆点与文字错位。  
**涉及文件**　`electron/renderer/styles.css`、`docs/更新记录.md`  
**发版**　否（刷新 renderer）

---

#### 2026-07-30 15:30 — 「欢迎回来」弹窗正文左对齐 · 已测试

**改了什么**　`.modal-card` 默认居中覆盖了 `modal-card-form`；补强 form 弹窗与 `#continuity-modal-body` 为左对齐。  
**为什么改**　续航摘要多行被居中，难读。  
**涉及文件**　`electron/renderer/styles.css`、`docs/更新记录.md`  
**发版**　否（刷新 renderer）

---

#### 2026-07-30 15:12 — 今日知签 / 库运势 / 定稿仪式 · 已测试

**改了什么**　新增「今日知签」（宜忌+座右铭）、基于近7日真实统计的「库运势」、八日签册本地记忆；提问续航条与记忆面板「知签」页；写文章可选「今日可归档」清单（默认关，不强制拦截）。  
**为什么改**　从合历类产品借「仪式停顿」与轻情绪价值，不做成迷信方法论。  
**涉及文件**　`lib/fortune.py`、`backend/server.py`、`lib/settings.py`、`electron/renderer/{index.html,app.js,styles.css}`、`docs/更新记录.md`  
**改后效果**　设置「对话与记忆」可关知签；开启定稿清单时未勾齐仅 confirm 提醒。  
**发版**　否（随后续安装包；须重启后端）

---

#### 2026-07-30 09:20 — GUI/CLI 一键生成目标任务书（leader）· 已测试

**改了什么**　`leader` Skill 增加 `action: leader`；`llm.generate_goal_brief` 按 anatomy 单轮生成并写入 `concepts/`；工作流卡片「生成目标任务书」。  
**为什么改**　最终用户需在易知 GUI 点一下即可生成可执行任务书，不必依赖 Cursor。  
**涉及文件**　`lib/llm.py`、`lib/skills.py`、`lib/workflows.py`、`skills/leader/`、`.cursor/skills/leader/`、`skills/README.md`、`docs/更新记录.md`  
**改后效果**　`yws skill run leader --topic "…"` 与 GUI 工作流可用；日志展示全文并可复制。  
**发版**　否（随后续安装包）

---

#### 2026-07-30 09:05 — 内置 leader Skill（目标任务书）· 已测试

**改了什么**　将 KKKKhazix `leader` Skill 写入易知 `skills/leader/`（含 `references/`），并同步 `.cursor/skills/leader/`；补充 `myknowledge.triggers`；`skills/README.md` 增加说明。  
**为什么改**　长任务（瘦身、换栈、大规模整理）需在开工前焊死目标与防作弊验收，避免 Agent 目标漂移。  
**涉及文件**　`skills/leader/`、`.cursor/skills/leader/`、`skills/README.md`、`docs/更新记录.md`  
**改后效果**　`yws skill show leader` / `yws skill match 写任务书` 可发现；Cursor 在易知仓库内可作项目 Skill 调用；无 `yws skill run`（方法论型）。  
**发版**　否

---

#### 2026-07-29 12:30 — 保留 mermaid/pdfjs-dist/three；旁路恢复开发依赖 · 已测试

**改了什么**　A5 prune 不再删除 `mermaid` / `pdfjs-dist` / `three`；开发树用旁路 `dist/_npm_restore_preview` 安装并合并进 `electron/node_modules`（避开占用中的 electron）。  
**为什么改**　用户预览链路仍需要这三包；此前 A5 误清导致缺失。  
**涉及文件**　`scripts/prune_electron_modules.ps1`、`docs/更新记录.md`  
**改后效果**　重打安装包时仍保留三包；开发机已恢复。  
**发版**　否

---

#### 2026-07-29 12:18 — 开发启动：vendor 已在则跳过 file-viewer npm · 已测试

**改了什么**　`launch-yizhi.bat`：若 `renderer/vendor/file-viewer` 已存在则不再因缺少 `node_modules/@file-viewer` 而 `npm install`；仅缺 vendor 时才装包并 sync。  
**为什么改**　A5 精简后开发树缺 `@file-viewer`，全量 `npm install` 会改名被占用的 `electron\default_app.asar` 报 EBUSY。  
**涉及文件**　`launch-yizhi.bat`、`docs/更新记录.md`  
**改后效果**　开发机可直接启动（vendor 已同步时）。  
**发版**　否

---

#### 2026-07-29 11:59 — 安装包瘦身 Sprint1：A1+A5+A6 · 已测试

**改了什么**　A1：安装 staging 默认删除重复 `python\`（`-KeepBundledPythonFallback` 可保留）；Inno/VBS/`main.js` 只认 `runtime\yizhi-backend.exe`。A5：`prune_electron_modules` 删重依赖并以 electron 依赖闭包白名单清孤儿。A6：Splash 分阶段文案；失败合并 backend/frozen 日志；无 python 时跳过 cli init。  
**为什么改**　按瘦身清单 Sprint 1 降体积与改善启动体感，不换栈。  
**涉及文件**　`scripts/{build-portable,build-installer,prune_install_staging,prune_electron_modules}.ps1`、`scripts/installer/{YizhiSetup.iss,YizhiStart.vbs}`、`electron/main.js`、`docs/commercial/{后端二进制化,构建安装包,安装包瘦身任务清单}.md`  
**改后效果**　须重打安装包后体积下降才体现在 Setup.exe；开发态不变。  
**发版**　否（待重打 Setup）

---

#### 2026-07-29 11:25 — 换栈可行性落地为决策文档（A/B 清单）· 已测试

**改了什么**　落盘决策基线（P0=方案 A 瘦身，P1=Tauri 壳+Python 后端；暂缓 C#/全量 Rust）；方案 A 可执行任务清单；方案 B Tauri 迁移规格；构建安装包文档入口链接。  
**为什么改**　换栈分析需可拍板、可排期，而非仅停留在对话。  
**涉及文件**　`docs/commercial/{换栈决策基线,安装包瘦身任务清单,Tauri壳迁移规格,构建安装包}.md`、`docs/更新记录.md`  
**改后效果**　不改打包默认行为；实施瘦身或 Tauri 时按清单/规格执行。  
**发版**　否（文档）

---

#### 2026-07-29 09:14 — Contrarian 矛盾检测 + RAG 父-子/冲突/HyDE · 已测试

**改了什么**　① 记忆面板「矛盾检测」：收集核心假设、一键扫描冲突并写对照稿（不合并）；设置项 `MYKNOWLEDGE_CONTRARIAN`（默认关）控制入库是否提炼假设。② RAG：默认开启父-子分块与提问冲突提示；可选 HyDE。  
**为什么改**　资源文中两条可落地增量：知识库自洽对照 + 检索质量。  
**涉及文件**　`lib/{rag_chunk,rag,rag_agent,config,settings,document_analysis,ingest,contrarian,llm}.py`、`backend/server.py`、`electron/renderer/{index.html,app.js}`、`tests/test_parent_child_contrarian.py`  
**改后效果**　开启父-子后建议重建索引；矛盾检测在「记忆与进化」页签可用。  
**发版**　否（客户端刷新/重启后端即可）

---

#### 2026-07-24 06:43 — 授权运营台「反馈」页签 + SQLite 固化 · 已测试

**改了什么**　运营台「运维」旁增加「反馈」页签；有网提交写入 `feedback` 表；`GET /admin/feedback` 列表/详情；历史 inbox 可幂等导入。  
**为什么改**　反馈需在授权服可查、可固化，不只发邮件。  
**涉及文件**　`license-server/app/db.py`、`feedback_store.py`、`main.py`、`static/admin/index.html`、`docs/commercial/反馈闭环.md`  
**改后效果**　部署重启后可在 `/license/admin/ui` →「反馈」查看用户反馈。  
**发版**　否（授权服热更）

---

#### 2026-07-24 06:39 — 反馈闭环：本地排队 + 服务端落盘发信 · 已测试

**改了什么**　客户端非空校验与本地 `.feedback_queue`；授权服先写 `feedback_inbox` 再限时发信；修复 agently `--body-file` 须相对路径导致发信失败；成功用 agently 发到 `ywsay@126.com`。  
**为什么改**　线上 `/feedback` 长时间挂起后 502，发信未真正打通。  
**涉及文件**　`lib/feedback.py`、`backend/server.py`、`electron/renderer/app.js`、`license-server/app/{main,feedback_mail,feedback_store}.py`、`tests/test_feedback_queue.py`、`docs/commercial/反馈闭环.md`  
**改后效果**　有网可投递邮箱；无网留本地并自动重试。**须把授权服新代码部署到生产并重启**后公网才生效。  
**发版**　否（授权服热更；客户端刷新即可）

---

#### 2026-07-24 06:23 — 反馈弹窗固定 4:3 加宽 · 已测试

**改了什么**　`.feedback-modal-card` 改为 `aspect-ratio: 4/3`，宽度上限约 1200px，并按 `90vh` 反推宽度，避免窄窗变成竖条。  
**为什么改**　原先固定高度 720px，窗口不够宽时比例偏竖。  
**涉及文件**　`electron/renderer/styles.css`、`docs/更新记录.md`  
**改后效果**　意见反馈弹窗更宽，桌面下保持约 4:3。  
**发版**　否（刷新界面即可）

---

#### 2026-07-23 07:55 — 记忆进化与启动续航（规则/摘要/召回/审计）· 已测试

**改了什么**　输出规则条目化（id/开关/删改）；`/api/memory/continuity` 与进化快照；提问页续航条 + 冷启动每日弹窗；会话摘要注入；「记忆与进化」面板；相关问答引用脚注与审计日志。  
**为什么改**　重启不迷路、偏好可治理，体现易知随使用进化。  
**涉及文件**　`lib/output_rules.py`、`lib/memory.py`、`lib/memory_db.py`、`lib/llm.py`、`backend/server.py`、`electron/renderer/index.html`、`app.js`、`styles.css`、`docs/superpowers/specs/2026-07-23-yizhi-memory-evolution-design.md`、`docs/superpowers/plans/2026-07-23-yizhi-memory-evolution.md`  
**改后效果**　继续上次会话；关规则不再注入；长对话可摘要；答案可列出参考历史问答；面板可删会话/看审计。  
**发版**　否（开发态刷新即可；安装包需重打）

---

#### 2026-07-23 07:11 — 写文章可选「第一性原理与对抗式审查」· 已测试

**改了什么**　「写文章」页增加开关（默认关）；启用后在撰写/改稿提示中注入「每次回答都要运用第一性原理，并进行对抗式审查」。  
**为什么改**　该思维约束仅适合部分稿件，不宜默认开启。  
**涉及文件**　`electron/renderer/index.html`、`electron/renderer/app.js`、`backend/server.py`、`lib/llm.py`、`docs/更新记录.md`  
**改后效果**　默认行为不变；勾选后写文章与按要求改稿均带上该句。  
**发版**　否（开发态刷新即可；安装包需重打才带上）

---

#### 2026-07-22 14:48 — 修复安装包渠道尾标导致安装中断 · 已测试

**改了什么**　`YizhiSetup.iss` 渠道尾标改为按字节安全读取 + 全路径 try/except；无尾标时不再抛 `Stream read error`。版本仍 **2.3.0**，已重编译 Setup。  
**为什么改**　刚打的包在「正在安装」末尾报 `Runtime error … Stream read error: 内存位置访问无效`。  
**涉及文件**　`scripts/installer/YizhiSetup.iss`、`dist/Yizhi-Setup-2.3.0.exe`  
**改后效果**　官方包（无渠道码）安装应能走完；推广打码包仍可读尾标。  
**发版**　是（覆盖）→ `dist\Yizhi-Setup-2.3.0.exe`  
**SHA256**　`17B14455CE91D8EE851FABD57DEBCC860E556F2AAB27D981183BDC06185BB343`

---

#### 2026-07-22 12:53 — 重打 2.3.0 安装包（含授权吊销启动修复）· 已测试

**改了什么**　版本仍为 **2.3.0**；修复 `YizhiSetup.iss` 渠道尾标 `ReadBuffer` Type mismatch；重新编译 Setup。  
**为什么改**　安装版遇 `license_revoked` 无法启动；需不升版本热修包。  
**涉及文件**　`scripts/installer/YizhiSetup.iss`、`lib/license_service.py`、`backend/server.py`、`dist/Yizhi-Setup-2.3.0.exe`  
**改后效果**　`dist\Yizhi-Setup-2.3.0.exe`（约 461MB）。  
**发版**　是（覆盖上传）→ `Yizhi-Setup-2.3.0.exe`（或 zip）+ 视需要更新 `latest.json`  
**SHA256**　`18E007C4E500EBCD4275B28811CE0401A1EF5F20DC9D0EB7B3636E3E6EA9080A`

---

#### 2026-07-22 11:51 — 授权吊销不再导致启动失败 · 已测试

**改了什么**　心跳遇到 `license_revoked` 等改为清本地态并返回失败，不再抛死；`startup` 对授权检查加防护。本机已备份并清除失效的 `.license/state.json`。  
**为什么改**　服务端许可证为 revoked 时，安装版后端启动直接退出，整窗报「易知启动失败」。  
**涉及文件**　`lib/license_service.py`、`backend/server.py`  
**改后效果**　本机可先启动进入激活；正式包需下一版才含代码防护。  
**发版**　否（热修建议下一补丁合入）

---

#### 2026-07-20 11:52 — 授权服务支持周易独立定价 · 已测试

**改了什么**　`create-order` / `plans` 增加 `product=yizhi|zhouyi`；周易价 9.9/39.9/99.9，标题「周易卦象订阅」。  
**为什么改**　周易桌面版复用虎皮椒通道但需独立定价，不能沿用易知 19/99/199。  
**涉及文件**　`license-server/app/config.py`、`db.py`、`main.py`、`.env`  
**改后效果**　本机 `product=zhouyi` 下单金额为 9.9；易知默认价不变。  
**发版**　是（须部署云主机 license-server）；changelog 不改（对易知用户无感知）

---

#### 2026-07-19 08:06 — Playwright Electron 宣传录屏脚本 · 已测试

**改了什么**　新增 `scripts/promo-record/`：自动启动易知、点选顶栏与各主 Tab，录制成 webm（有 ffmpeg 再转 mp4）。  
**为什么改**　需要可重复的功能演示视频用于宣传。  
**涉及文件**　`scripts/promo-record/record-demo.cjs`、`record-promo.ps1`、`package.json`  
**改后效果**　本机试跑产出约 3.5MB `dist/promo-videos/yizhi-promo-*.webm`。  
**发版**　否（开发工具，不进安装包）

---

#### 2026-07-19 07:34 — 反馈弹窗加宽（修 max-width 被卡住）· 已测试

**改了什么**　`.feedback-modal-card` 显式覆盖 `.modal-card` 的 `max-width:420px`，宽度改为约 `1080px / 94vw`。  
**为什么改**　反馈窗继承关于类弹窗的 420px 上限，实际比设定的 880px 更窄。  
**涉及文件**　`electron/renderer/styles.css`  
**改后效果**　意见反馈弹窗明显加宽，仍不超过视口且不出现横向滚动条。  
**发版**　否（热修）

---

#### 2026-07-19 07:26 — 全局禁止横向滚动条（资料库等）· 已测试

**改了什么**　主界面与滚动容器统一 `overflow-x: hidden`；资料库表格 `table-layout:fixed` + 标题省略，操作列可换行；隐藏 WebKit 横向滚动条；代码块改为折行。  
**为什么改**　资料库列表在标题较长时底部出现横向滚动条，与产品「不出现横滑条」要求不符。  
**涉及文件**　`electron/renderer/styles.css`、`electron/renderer/viewer.html`  
**改后效果**　资料库及各面板仅保留纵向滚动；长标题以省略号截断。  
**发版**　否（热修/下一补丁）

---

#### 2026-07-18 22:30 — 顶栏「反馈」+ 服务器代发邮件（离线排队）· 已测试

**改了什么**　顶栏最右侧增加「反馈」富文本弹窗；本地排队后 POST 至授权服务 `/feedback`，由服务器用 SMTP 或 agently-cli（`suddly@agent.qq.com`）发往 `ywsay@126.com`；无网自动补发。  
**为什么改**　便于用户提交 Bug/建议，且发件密钥不进安装包。  
**涉及文件**　`license-server/app/feedback_mail.py`、`main.py`、`config.py`、`.env.example`；`lib/feedback.py`；`backend/server.py`；`electron/renderer/index.html`、`app.js`、`styles.css`；`tests/test_feedback_queue.py`  
**改后效果**　客户端可提交；服务器需部署新代码并配置 SMTP 或登录 agently。  
**发版**　否（客户端随下一版；授权服务需先更新）

---

#### 2026-07-18 22:11 — 修复增强抓取缺 bun（公众号等）· 已测试

**改了什么**　`setup_optional_tools` 默认会安装/复制 `third_party/bun/bun.exe`（与 yt-dlp 同策略）；构建断言必须含 bun；缺 bun 时公众号/增强抓取给出明确提示。开发机已装好 bun。  
**为什么改**　baoyu-fetch 已在包内，但缺 bun 时增强抓取静默跳过，设置页总提示「未安装 bun」。  
**涉及文件**　`scripts/setup_optional_tools.py`、`scripts/build-portable.ps1`、`lib/url_fetch.py`、`.gitignore`  
**改后效果**　设置→能力检测应显示 bun OK；公众号链接可走 baoyu。已安装用户可「一键修复可选工具」或重装下一版。  
**发版**　否（热修/下一补丁；正式包须含 bun）

---

#### 2026-07-18 20:10 — 发布 2.3.0 · 已测试

**改了什么**　版本升至 2.3.0；用户向 changelog / latest.json 同步；安装包 `Yizhi-Setup-2.3.0.exe`（含写文章改稿闭环、B 站字幕+yt-dlp、安装版稳定性修复）。  
**为什么改**　重要能力合集正式发版。  
**涉及文件**　`electron/package.json`、`package-lock.json`、`YizhiSetup.iss`、`docs/commercial/changelog.html`、`yizhi-web/latest.json`、`docs/更新记录.md`  
**改后效果**　检查更新指向 2.3.0；关于→更新记录含本版说明。  
**发版**　是 → `dist\Yizhi-Setup-2.3.0.exe`；须上传 `yizhi/`：exe、`latest.json`、`changelog.html`  
**SHA256**　`D53FAD8A34C7A73E88FC33BEBFBD4127BF392CB89CA1FABF1587BCE78EECC1A9`

---

#### 2026-07-18 17:39 — 写文章：草稿可改 + LLM 改稿 + 确认定稿 · 已测试

**改了什么**　生成后进入可编辑草稿（Toast UI），支持手改与「按要求修改」（可选重 RAG）；去掉生成即入库；「确认定稿」才写入知识库，可继续修改后覆盖同页。  
**为什么改**　用户需在满意前反复打磨成稿，而不是一次生成即落库。  
**涉及文件**　`lib/llm.py`、`backend/server.py`、`electron/renderer/index.html`、`app.js`、`styles.css`、`tests/test_produce_revise.py`  
**改后效果**　开始写 → 编辑/改稿 → 定稿入库；`POST /api/produce/revise`、`/finalize`。  
**发版**　否（开发机热重载/重启后端即可测）

---

#### 2026-07-18 16:59 — B 站链接字幕入库 + 安装包内置 yt-dlp · 已测试

**改了什么**　「保存网页」识别 bilibili.com / b23.tv，用 yt-dlp 仅取字幕入库并走现有 RAG；无字幕明确失败。安装流程打包 `third_party/yt-dlp/yt-dlp.exe`，启动 PATH 含该目录。  
**为什么改**　用户需把有字幕的 B 站视频进知识库，且正式安装包须自带 yt-dlp。  
**涉及文件**　`lib/bilibili_fetch.py`、`lib/url_fetch.py`、`lib/runtime_tools.py`、`scripts/setup_optional_tools.py`、`scripts/build-portable.ps1`、`scripts/installer/YizhiStart.vbs`、`tests/test_bilibili_fetch.py`、`electron/renderer/index.html`  
**改后效果**　粘贴 B 站链接即可入库；构建时缺 yt-dlp 会失败并提示。  
**发版**　否（待下一正式版打进安装包；开发机已可测）

---

#### 2026-07-16 02:32 — 修复安装版资料库「浏览」预览失败 · 已测试

**改了什么**　`backend/server.py` 预览静态路径改用 `lib.config.ROOT`；安装版错误文案不再提 Node/启动 bat。  
**为什么改**　冻结后端仍用 `__file__` 推根目录，`/api/health.file_viewer.ready=false`，点击浏览报「未能自动安装」。  
**涉及文件**　`backend/server.py`、`electron/renderer/app.js`、`scripts/hotpatch-install-2.2.0.ps1`  
**改后效果**　health 能找到 `electron\renderer\vendor\file-viewer`，预览可开。  
**发版**　否（热修；请管理员跑 `hotpatch-install-2.2.0.ps1` 后重启易知）

---

#### 2026-07-16 00:30 — 新增《发版操作手册》· 已测试

**改了什么**　新增 `docs/commercial/发版操作手册.md`：升版本→构建→验收→上传→验证的逐步说明；`构建安装包.md` / `发布前最后三步.md` 增加入口链接。  
**为什么改**　发版步骤分散，需要一份可照做的完整顺序文档。  
**涉及文件**　`docs/commercial/发版操作手册.md`、`构建安装包.md`、`发布前最后三步.md`  
**改后效果**　按手册 ①～⑧ 即可完成正式发版。  
**发版**　否

---

#### 2026-07-16 00:15 — 修复安装版默认工作流缺失 · 已测试

**改了什么**　冻结后端正确解析应用根目录（`MYK_ROOT` / `runtime` 上级）；自定义工作流写入用户数据目录；Electron 注入 `MYK_ROOT`。  
**为什么改**　安装版 `ROOT` 落在 `runtime\_internal`，找不到 `skills/`，导入资料显示「暂无工作流」。  
**涉及文件**　`lib/config.py`、`lib/workflows.py`、`electron/main.js`；热修脚本 `scripts/hotpatch-install-2.2.0.ps1`  
**改后效果**　内置工作流卡片恢复（整理待处理、更新检索库等）。  
**发版**　否（热修；可随下一补丁包）

---

#### 2026-07-15 23:52 — 修复安装版笔记「保存」无反应 · 已测试

**改了什么**　笔记 PUT/POST 由全库 `sync_all()` 改为单篇增量索引；保存按钮显示「保存中…」与成功 Toast；主题切换后笔记编辑器用 live 实例。  
**为什么改**　`sync_all` 阻塞请求导致点击保存像无响应；此前缺少明确保存反馈。  
**涉及文件**　`backend/server.py`、`electron/renderer/app.js`、`electron/renderer/rich-editor.js`  
**改后效果**　保存秒回；按钮有「保存中…」→ Toast「笔记已保存」。  
**发版**　否（热修；随后可打补丁版安装包）

---

#### 2026-07-15 23:20 — 发布 2.2.0（资料夹 + 后端二进制 + 启动/闪念等）· 已测试

**改了什么**　版本升至 **2.2.0**；资料夹 CRUD/多选/问答范围；`runtime\yizhi-backend.exe`；静默启动与闪念修复；工作流 skills 打包；播客语气；设置资料夹选项卡。  
**为什么改**　重要功能版本：知识组织与安装包形态升级，汇总近期已验证改动一并发行。  
**涉及文件**　`electron/package.json`、`package-lock.json`、`YizhiSetup.iss`、`lib/folders.py`、`backend/`、`electron/main.js`、`YizhiStart.vbs`、`scripts/build-*.ps1`、`docs/commercial/*`  
**改后效果**　检查更新指向 2.2.0；新安装包含后端二进制与资料夹能力。  
**发版**　是 → `dist\Yizhi-Setup-2.2.0.exe`（约 440.6 MB，SHA256 `B97FBE0F8790E2BAB01BB10E79F16D35367AFB24CFE2200E8F65046F6F58D91B`）；待上传 `yizhi/`：exe、`latest.json`、`changelog.html`

---

#### 2026-07-14 16:45 — 发布 2.1.2，标记 2.1.1 为失败版本 · 已测试

**改了什么**　版本升至 **2.1.2**；`changelog.html` / `latest.json` 标明 2.1.1「失败版本·已撤回」；`update_check` 识别 `recalled_versions` 并提示强制升级。  
**为什么改**　2.1.1 安装启动、编辑器、ffmpeg 等问题频发，产品定为失败，须停止分发并以 2.1.2 替代。  
**涉及文件**　`electron/package.json`、`package-lock.json`、`YizhiSetup.iss`、`backend/server.py`、`docs/commercial/yizhi-web/latest.json`、`changelog.html`、`lib/update_check.py`、`electron/renderer/app.js`  
**改后效果**　运行 2.1.1 检查更新会警示撤回并引导下载 2.1.2。  
**发版**　是 → 重打 `Yizhi-Setup-2.1.2.exe`；上传 `yizhi/`：exe、`latest.json`、`changelog.html`；勿再分发 2.1.1

---

#### 2026-07-14 16:35 — 播客合并 ffmpeg failed：只拷了 exe 缺 DLL · 已测试

**改了什么**　`setup_optional_tools` 拷贝 ffmpeg **整个 bin**（含 avcodec 等 DLL），并用 `-version` 校验；`podcast_merge` 同格式优先 `-c copy`，缺 DLL 时给出明确错误。  
**为什么改**　安装包只含 `ffmpeg.exe`/`ffprobe.exe`，客户机运行码 `0xC0000135`，合并报 `ffmpeg failed`。  
**涉及文件**　`scripts/setup_optional_tools.py`、`lib/podcast_merge.py`  
**发版**　客户机可先把 `C:\ffmpeg\bin\*`（或开发机完整 bin）拷到 `D:\Yizhi\third_party\ffmpeg\bin\` 后重试播客；正式包须重打

---

#### 2026-07-14 16:25 — 笔记编辑器选中不显示：隐藏面板内创建 ToastUI · 已测试

**改了什么**　笔记正文改为切到「编辑器」时懒创建；`selectPage` 补错误提示与未知 type 选项；选中后 `fitToParent`。  
**为什么改**　默认时间线视图下 `edit-body-host` 在 `display:none` 中初始化，ToastUI 高度异常，左侧选中后正文空白。  
**涉及文件**　`electron/renderer/app.js`  
**发版**　覆盖客户机 `app.js`（建议连同上一版 `rich-editor.js`）后重启

---

#### 2026-07-14 16:15 — 闪念点保存无反应：rich-editor 重复 create · 已测试

**改了什么**　删除 `rich-editor.js` 中后一个无 try/catch 的 `create`（覆盖了带纯文本回退的版本）；保存空内容时提示「请先输入闪念内容」；初始化编辑器失败弹窗。  
**为什么改**　客户机 ToastUI 创建失败时异常未捕获 → `memoEditorRec` 为空 → 点保存静默 return。  
**涉及文件**　`electron/renderer/rich-editor.js`、`app.js`  
**发版**　可先覆盖客户机 `D:\Yizhi\electron\renderer\` 下上述文件后重启验证，再打正式包

---


**改了什么**　`YizhiStart.bat` 改为 `start "" "%EL_EXE%"`（空窗口标题）；先检查 `default_app.asar`。  
**为什么改**　`start "Yizhi" /D ...` 在部分环境下被解析成访问网络名 `yizhi`（错误 0x80070043）。  
**涉及文件**　`scripts/build-portable.ps1`  
**发版**　客户机可先替换 `YizhiStart.bat`；并确保已有 `default_app.asar`

---


**改了什么**　`prune_electron_modules.ps1` **不再删除** `electron/dist/resources/default_app.asar`；启动窗不透明 + boot 日志仍保留。  
**为什么改**　删掉该文件后 `electron.exe` 直接异常退出/光标闪、**不加载 main.js**（本机复现：补回 asar 后即可出「易知」窗口）。  
**涉及文件**　`scripts/prune_electron_modules.ps1`、`electron/main.js`、`electron/device-id.js`  
**发版**　客户机可先手动拷回 `default_app.asar`；正式包须重编（勿再用会删 asar 的旧 prune）

---


**改了什么**　启动窗改为不透明并立即 `show:true`；`device-id` 用 `reg/wmic` 替代易挂起的 PowerShell；写 `%LOCALAPPDATA%\Yizhi\electron-boot.log` 定位卡点。  
**为什么改**　客户机 `electron.exe .` 控制台光标一直闪、无界面——透明窗不触发显示，且指纹 PowerShell 可挂起。  
**涉及文件**　`electron/main.js`、`electron/device-id.js`  
**发版**　客户机先覆盖这两个文件验证，再重编 Setup

---


**改了什么**　`killPort` 不再调用 `Get-NetTCPConnection`（可永久挂起），改 `netstat`+超时；检测内置 Python 即按安装包路径用 `%LOCALAPPDATA%\Yizhi`；启动时尽早出 splash，失败一律 ErrorBox；PATH 优先内置 Python。  
**为什么改**　`D:\Yizhi` 下 `electron.exe .` 光标闪、无界面——多半卡在释放端口的 PowerShell。  
**涉及文件**　`electron/main.js`  
**发版**　须把 `main.js` 打进新 Setup（或先拷到客户机 `D:\Yizhi\electron\main.js` 验证）

---


**改了什么**　bat 不再依赖静默 VBS；`start electron.exe .` 后 `tasklist` 探测；秒退则前台重跑并 `pause`，日志写 `%LOCALAPPDATA%\Yizhi\`。  
**为什么改**　客户机三文件齐全但双击 bat 一闪而过——旧 bat 调 VBS 后立即退出，Electron 崩了无窗口。  
**涉及文件**　`scripts/build-portable.ps1`  
**发版**　须重编 Setup；客户机可先手动替换安装目录 `YizhiStart.bat` 试跑

---


**改了什么**　`YizhiStart.bat` 入口即 `mkdir` 数据目录并写 `launcher.log`；缺 `python.exe`/`electron.exe` 时 `echo`+`pause`，不再静默。  
**为什么改**　客户机无 `%LOCALAPPDATA%\Yizhi` 说明旧快捷方式（`wscript //B`）几乎未真正跑到建目录；需可观测启动链。  
**涉及文件**　`scripts/build-portable.ps1`（生成 bat）  
**发版**　须重编 Setup（可 `-SkipPortableBuild`，staging 已同步 bat/vbs）

---


**改了什么**　快捷方式/开机启动改为 `YizhiStart.bat`（不再 `wscript //B`）；VBS 检查 `python\python.exe`、启动后 2s 探测 electron 进程，失败弹窗并写 `%LOCALAPPDATA%\Yizhi\launcher.log`。  
**为什么改**　`//B` 吞掉 MsgBox，缺运行时/VC++ 秒退时表现为「点了没反应」；旧自检也未覆盖可搬迁 Python 路径。  
**涉及文件**　`scripts/installer/YizhiSetup.iss`、`YizhiStart.vbs`、`scripts/build-portable.ps1`  
**发版**　须重编 Setup（`-SkipPortableBuild` 前请把修好的 `YizhiStart.vbs`/`.bat` 拷进 staging，或整包重打）

---

#### 2026-07-14 15:05 — 安装完成误报「未检测到内置 Python」· 已测试

**改了什么**　Inno `BundledRuntimeOk` 改为认 `python\python.exe`（可搬迁布局），并兼容旧 `python\Scripts\python.exe`。  
**为什么改**　打包已改为整份 Python，安装后自检仍查 venv 路径，导致假警报。  
**涉及文件**　`scripts/installer/YizhiSetup.iss`  
**发版**　须用修好的 iss **重新编译** Setup（不必重打 staging：`-SkipPortableBuild` 即可）

---

#### 2026-07-14 14:20 — 可选工具：优先复制本机 ffmpeg/bun，默认不访问 GitHub · 已测试

**改了什么**　`setup_optional_tools` 从本机 PATH/`C:\ffmpeg\bin`/npm bun **复制进** `third_party`；默认关闭 GitHub 下载（`MYKNOWLEDGE_ALLOW_GITHUB_TOOLS=1` 才开）；`find_ffmpeg` 增加常见路径。  
**为什么改**　内置 Python 已卸 pip；开发机有 ffmpeg 但不在 PATH 时旧逻辑会去 GitHub 超时；且只「找到」不打包会导致用户机缺工具。  
**涉及文件**　`scripts/setup_optional_tools.py`、`lib/runtime_tools.py`、`docs/commercial/构建安装包.md`  
**发版**　否（构建工具）；须重跑 build-installer（staging 尚缺 `.env.user` / 启动脚本时不可仅 `-SkipPortableBuild`）

---

#### 2026-07-14 14:05 — 构建脚本：$PyExe 污染加固 + Push-Location 崩溃复原 · 已测试

**改了什么**　pip/robocopy 改 `Out-Host`；`Install-BundledPython` 无返回值；`$PyExe` 只从磁盘 `Resolve-BundledPythonExe`；可选工具/init_wiki 用 try/finally Pop-Location。  
**为什么改**　赋值仍被 pip 成功列表污染；失败后停在 `dist\Yizhi-Staging-*` 目录。  
**涉及文件**　`scripts/build-portable.ps1`  
**发版**　否（构建工具）；须重跑 build-installer

---

#### 2026-07-14 13:40 — 安装包最小依赖：runtime Python + 精简 Electron · 已测试

**改了什么**　新增 `requirements-runtime.txt`（构建只装它）；`requirements.txt` 拆出 OCR 等可选；Electron 把 toastui/d3/file-viewer/marked 改 `devDependencies`，sync 到 vendor 后剪掉；新增 `prune_electron_modules.ps1`（语言包仅中英）；Python 拷贝排除 Tcl/Tk/idle，装完卸 pip。  
**为什么改**　用户要求库最小化，非必要不打包。  
**涉及文件**　`requirements-runtime.txt`、`requirements.txt`、`requirements-optional.txt`、`build-portable.ps1`、`prune_electron_modules.ps1`、`prune_install_staging.ps1`、`electron/package.json`、`docs/commercial/构建安装包.md`  
**发版**　随 **2.1.1** 重打安装包

---

#### 2026-07-14 13:30 — 构建卡死：拷贝 Python 跳过 site-packages · 已测试

**改了什么**　`Install-BundledPython` 用 robocopy `/XD site-packages` + `/MT:16`，不再拷贝开发机数 GB 全局包；并显示进度摘要。  
**为什么改**　整份拷贝 base_prefix 时把开发机 site-packages（可达 5GB+）也拷进 staging，界面像卡住。  
**涉及文件**　`scripts/build-portable.ps1`  
**发版**　否（构建工具）；打 **2.1.1** 前须用本修复重跑构建

---

#### 2026-07-14 12:52 — 版本号升至 2.1.1 · 已测试

**改了什么**　`electron/package.json` → `2.1.1`；同步 `YizhiSetup.iss` 默认版本、`yizhi-web/latest.json`、用户向 `changelog.html`。  
**为什么改**　发包含可搬迁 Python 与它机启动修复的正式包。  
**涉及文件**　`electron/package.json`、`package-lock.json`、`backend/server.py`、`scripts/installer/YizhiSetup.iss`、`docs/commercial/yizhi-web/latest.json`、`docs/commercial/changelog.html`  
**发版**　是 → **2.1.1** 安装包（须重新 build-installer）

---

#### 2026-07-14 08:35 — 安装包 Python 可搬迁（修复它机启动失败） · 已测试

**改了什么**　`build-portable` 改为整份拷贝 `sys.base_prefix`（`python\python.exe`），废除 `venv`；构建门禁拒绝带 `pyvenv.cfg` 的包；启动器/`main.js` 优先找 `python\python.exe`。  
**为什么改**　旧包 `pyvenv.cfg` 写死开发机 Python 路径，开发机正常、其它电脑（任意安装路径）启动失败（常报 exit 103 / 知识库初始化失败）。  
**涉及文件**　`scripts/build-portable.ps1`、`build-installer.ps1`、`electron/main.js`、`launch-yizhi.bat`、`YizhiStart.vbs`、`docs/commercial/构建安装包.md`  
**发版**　**须重新** `build-portable` + `build-installer`，上传新 Setup；旧安装包无法仅用补丁修复内置 Python

---

#### 2026-07-14 08:20 — 启动失败：Program Files 无写权限 mkdir skills · 已测试

**改了什么**　`init_wiki.py` 不再因无法在安装目录创建 `skills/` 而失败（改为 wiki 目录创建 + 安装目录可写则建、否则跳过）；修复 `import sys` 遮蔽导致 memory 初始化跳过；`cmd_init` 在 inbox 已就绪时容忍次要失败；Electron `runPython` 输出 stderr；启动判定放宽为 inbox/notes/purpose.md 任一存在即跳过 init。  
**为什么改**　安装版在 Program Files 下首次初始化时报 `PermissionError`，表现为「知识库初始化失败: python exited with code …」。  
**涉及文件**　`scripts/init_wiki.py`、`lib/cli.py`、`electron/main.js`、`scripts/installer/patch-installed.ps1`  
**发版**　须管理员执行 `.\scripts\installer\patch-installed.ps1` 或重打安装包；客户端侧

---

#### 2026-07-13 23:50 — 授权统计 stats + 活跃设备 last_seen_at · 已测试

**改了什么**　`license_admin.py stats`（订阅/收入按日周月季年、活跃设备）；`export-csv` 含 events、支持 `--split`；heartbeat 写入 `licenses.last_seen_at`（启动/CLI 自动补列）；`GET /admin/stats`；文档 `docs/commercial/授权统计与报表.md`。  
**为什么改**　运营需查看订阅分类、收益与近似活跃数，不必手工 SQL。  
**涉及文件**　`license-server/app/db.py`、`stats.py`、`main.py`、`scripts/license_admin.py`、`tests/test_license_stats.py`、`tests/test_license_db_heartbeat.py`、`docs/commercial/授权统计与报表.md`、`docs/易知授权运营.md`、`license-server/README.md`  
**发版**　须**重启生产授权服务**并同步上述文件；客户端无需更新

---

#### 2026-07-13 22:53 — 关于页次要按钮单行紧凑 · 已测试

**改了什么**　「检查更新 / 购买说明 / 常见问题 / 更新记录 / 新手指引」改为单行不换行；字号 11px、浅底细边框，与上方续费/换机按钮区分。  
**涉及文件**　`electron/renderer/styles.css`  
**发版**　否，刷新 renderer 即可

---

#### 2026-07-13 21:35 — 关于页「更新记录」+ 服务器 changelog 页 · 已测试

**改了什么**　新增用户向 `docs/commercial/changelog.html`（按版本汇总）；`commercial_links` 增加 `changelog_url`；关于弹窗加「更新记录」按钮；构建脚本复制 changelog；上传说明补充手动同步。  
**为什么改**　用户需查看版本演进；与购买页/FAQ 一样由服务器托管，发版时手动上传即可。  
**涉及文件**　`docs/commercial/changelog.html`、`lib/commercial_links.py`、`env.user.example`、`index.html`、`app.js`、`build-portable.ps1`、`purchase.html`、`faq.html`、`docs/commercial/构建安装包.md`  
**发版**　须上传 `changelog.html` 至服务器 `yizhi/`；客户端刷新 renderer 或重装后「关于 → 更新记录」生效

---

#### 2026-07-13 15:00 — 人设「基本信息」：主人指代与检索增强 · 已测试

**改了什么**　人设弹窗新增最左「基本信息」Tab（姓名/性别/昵称/联系方式/头像/单位/别名等）；注入提问/推演/查询的系统提示与 RAG 扩展（「我」→ 主人姓名/闪念/笔记）；头像上传 API。  
**为什么改**　用户希望「我」与库内资料、闪念时间线深度关联，而非泛称。  
**涉及文件**　`lib/persona_config.py`、`prompts.py`、`rag_agent.py`、`llm.py`、`deduce.py`、`backend/server.py`、`index.html`、`app.js`、`styles.css`、`tests/test_persona_owner.py`  
**发版**　随 **2.1.0**（须重启后端）

---

#### 2026-07-13 14:22 — 推演关系图随窗口缩放自适应 · 已测试

**改了什么**　窗口或面板尺寸变化时重新 fit-to-view 填满容器；监听 graph 外层卡片与 window resize；切换 Tab 回来时触发 relayout。  
**为什么改**　首次 fit 后 `fitted` 阻止再次缩放，放大窗口后关系图仍缩在左上角。  
**涉及文件**　`electron/renderer/deduce-graph.js`  
**发版**　随 **2.1.0**（刷新 renderer）

---

#### 2026-07-13 14:18 — 上传卡在「正在更新检索库 92%」：改后台异步建索引 · 已测试

**改了什么**　上传完成后不再同步等待向量嵌入 API；改为快速重建 chunk 索引 + FTS，向量与 index 汇总页后台更新；合并建索引失败时保证唤醒等待任务。  
**为什么改**　`sync_all` 含全量 embedding，API 慢或不可达时任务长期停在 92%。  
**涉及文件**　`lib/rag.py`、`lib/ingest.py`、`lib/upload_jobs.py`  
**发版**　随 **2.1.0**（须重启后端）

---

#### 2026-07-13 14:05 — 上传任务 99% 卡住：索引阶段提示 + 合并重建 · 已测试

**改了什么**　文件入库完成后显示「正在更新检索库…」（不再误显示文件名 + 99%）；多个上传任务合并为一次检索库重建；任务中心进度改为入库 0–90%、建索引 92%、完成 100%。  
**为什么改**　入库后 `sync_all` 耗时长且前端硬封顶 99%，多次上传会排队重复建索引，用户以为卡住。  
**涉及文件**　`lib/upload_jobs.py`、`electron/renderer/jobs-ui.js`、`app.js`  
**发版**　随 **2.1.0**

---

#### 2026-07-13 13:52 — 导入资料工作流：运行按钮加载态 · 已测试

**改了什么**　工作流卡片点「运行」后按钮变为「工作中…」并禁用，卡片高亮；任务完成（含网页抓取后台任务）后自动复原；说明文案改为如何添加自定义快捷方式。  
**为什么改**　点击后无反馈，用户不确定是否在执行。  
**涉及文件**　`electron/renderer/app.js`、`styles.css`、`index.html`  
**发版**　随 **2.1.0**

---

#### 2026-07-13 13:45 — 推演关系图：大模型推断关系 + 箭头有向边 · 已测试

**改了什么**　检索后增加大模型关系推断（`deduce_relations.py`），从片段中抽取有向边并合并进子图；关系图用箭头连接、边 label 标注关系类型；wiki 链路边虚线、推断边实线蓝色。  
**为什么改**　仅 wiki links 时节点孤立，看不出结构；用户要求深度分析真实关系并用箭头体现。  
**涉及文件**　`lib/deduce_relations.py`、`lib/deduce_graph.py`、`lib/deduce.py`、`lib/prompts.py`、`electron/renderer/deduce-graph.js`、`tests/test_deduce_graph.py`  
**发版**　随 **2.1.0**

---

#### 2026-07-13 13:26 — 推演关系图闪动修复 · 已测试

**改了什么**　关系图 ResizeObserver 不再全量重绘（改为尺寸变化 ≥8px 时原地调整）；相同 graph 跳过重绘；仿真结束即停止 tick；报告区 `scrollbar-gutter: stable` 避免滚动条引起左侧宽度抖动。  
**为什么改**　右侧报告流式输出时滚动条出现/消失，触发左侧反复重绘导致闪动。  
**涉及文件**　`electron/renderer/deduce-graph.js`、`styles.css`  
**发版**　随 **2.1.0**

---

**改了什么**　输入框 placeholder 示例改为「根据工作记录，后续有哪些重点？」；关系图与推演报告拆为两个独立卡片，中间 **10px** 间距，右侧加「推演报告」标题。  
**涉及文件**　`electron/renderer/index.html`、`styles.css`  
**发版**　随 **2.1.0**

---

**改了什么**　推演结果改左右等高（左图右文）；关系图用固定像素尺寸 + 初始环形布局 + 多次 fit-to-view；新增「下载」按钮（生成前禁用），导出 Word 含关系图 PNG + 报告，页眉「易知」、页脚页码。API：`POST /api/deduce/export/docx`。  
**为什么改**　上图下文导致关系图高度塌陷无法显示；用户需导出完整推演文档。  
**涉及文件**　`index.html`、`styles.css`、`app.js`、`deduce-graph.js`、`lib/deduce_export.py`、`backend/server.py`  
**改后效果**　左关系图自动缩放完整可见，右 HTML 报告；完成后可下载 Word。  
**发版**　随 **2.1.0**

---

**改了什么**　输入框与「开始推演」合并为同行表单组（等高 44px）；关系图与文字报告合并为单一结果卡片（上图下文）；流式/完成态报告改 HTML 排版（marked）；关系图增加 fit-to-view 与 ResizeObserver。  
**为什么改**　原左右分栏导致图被裁切、报告为纯文本，输入与按钮高度不一致。  
**涉及文件**　`electron/renderer/index.html`、`styles.css`、`app.js`、`deduce-graph.js`  
**改后效果**　单行表单输入；统一结果区内关系图自动缩放完整可见，下方为排版后的报告。  
**发版**　随 **2.1.0** 安装包或 dev 刷新

---

**改了什么**　新增 `scripts/build-electron.ps1`（npm install + vendor 同步）；`electron/package.json` 增加 `build`/`sync-d3`；`build-portable.ps1` staging 内同步改为 `npm run build`（含 d3）。文档补充「先 build-electron 再打安装包」。  
**为什么改**　打安装包前先在开发树验证 Electron 与 vendor 完整，避免 staging 跑到一半才发现缺依赖。  
**涉及文件**　`scripts/build-electron.ps1`、`scripts/build-portable.ps1`、`electron/package.json`、`docs/commercial/构建安装包.md`  
**改后效果**　`.\scripts\build-electron.ps1` 通过后 staging/安装包可继续构建。  
**发版**　否（构建工具）

---

**改了什么**　`YizhiStart.vbs` 改以 `WshShell.Run`（设 `CurrentDirectory=electron`）为主启动；WMI `Win32_Process.Create` 改 `Nothing` 并校验返回码作兜底；失败写 `%LOCALAPPDATA%\Yizhi\launcher.log` 并弹窗。快捷方式/自启/安装后运行改用 `{sys}\wscript.exe //B`。  
**为什么改**　原 WMI 传 `Null` 且不查返回值，失败时桌面双击无任何反馈。  
**涉及文件**　`scripts/installer/YizhiStart.vbs`、`scripts/installer/YizhiSetup.iss`  
**改后效果**　双击桌面/开始菜单快捷方式应正常启动（Splash → 主窗）；失败时有提示与日志。  
**发版**　须重打安装包，或已安装环境运行 `scripts/installer/patch-installed.ps1`（管理员）

---

**改了什么**　`YizhiSetup.iss` 显式写入 `VersionInfoVersion`（semver → 四段，如 2.1.0.0）、`VersionInfoDescription`（易知 安装程序）、`VersionInfoCompany/ProductName`（易知）；发布者由「易文胜」改为「易知」。  
**为什么改**　资源管理器属性中文件版本曾显示 0.0.0.0，公司与产品名未统一为易知品牌。  
**涉及文件**　`scripts/installer/YizhiSetup.iss`、`docs/commercial/构建安装包.md`  
**改后效果**　重打安装包后，exe 属性：文件说明「易知 安装程序」、公司「易知」、文件版本与 `package.json` 一致。  
**发版**　须重跑 `build-installer.ps1` 后随 **2.1.0** 安装包生效

---

#### 2026-07-13 10:36 — 「推演」Tab 2.1.0 · 已测试

**改了什么**　新增「推演」Tab：自然语言输入 → 本地 RAG 检索 → 左侧 D3 关系子图 + 右侧推演报告与图谱解读 → 默认保存为笔记（`deduce: true`，tag `推演`）。后端 `POST /api/deduce/stream` SSE 流式；版本升至 **2.1.0**。  
**为什么改**　在本地知识库内做有依据的情景推演，输出关系图与文字报告，便于复盘与入库。  
**涉及文件**　`lib/deduce_graph.py`、`lib/deduce.py`、`lib/prompts.py`、`prompts/deduce.md`、`backend/server.py`、`electron/renderer/index.html`、`app.js`、`deduce-graph.js`、`styles.css`、`electron/package.json`、`electron/scripts/sync-d3.mjs`、`tests/test_deduce_graph.py`  
**改后效果**　主菜单「推演」→ 输入问题 → 开始推演 → 见关系图与报告 → 自动写入 `notes/`。  
**发版**　是，**2.1.0** 安装包

---

#### 2026-07-13 09:47 — 更新记录排版紧凑化 · 已测试

**改了什么**　`docs/更新记录.md` 条目改 `####` 标题；子项改加粗标签单行；减少空行与 `###` 层级。  
**为什么改**　原 `##`/`###` 在预览中字号过大、段间距过宽。  
**涉及文件**　`docs/更新记录.md`、`.cursor/rules/yizhi-update-log.mdc`  
**改后效果**　一屏可读更多条目；后续 Agent 按新模板追加。  
**发版**　否（文档）

---

#### 2026-07-13 09:41 — 闪念折叠第二行显示不全 · 已测试

**改了什么**　折叠态改为 `-webkit-line-clamp: 2`，清除 `<p>` margin，两行完整显示后再截断。  
**为什么改**　原 `max-height: calc(1.55em * 2)` 未计段落 margin，第二行被拦腰裁切。  
**涉及文件**　`electron/renderer/styles.css`  
**改后效果**　折叠时两行完整可见，第三行起隐藏；「更多」不变。  
**发版**　否，随 **2.0.1** 打包

---

#### 2026-07-13 09:39 — 闪念时间线默认两行折叠 · 已测试

**改了什么**　时间线闪念默认 2 行；超出折叠；图片等隐藏；「更多」/「收起」展开。  
**为什么改**　长闪念占满时间线，列表冗长。  
**涉及文件**　`electron/renderer/app.js`、`electron/renderer/styles.css`  
**改后效果**　短文无按钮；长文/含图默认 2 行 +「更多」。  
**发版**　否，随 **2.0.1** 打包

---

#### 2026-07-13 09:16 — 新手指引 v2 + 富文本弹层 · 已测试

**改了什么**　① 指引 v2（`myk-onboarding-v2`）：先导入/笔记再提问，结束跳导入页。② 工具栏弹层（图/链接）不再被裁切。  
**为什么改**　新用户空库提问；插入图片弹层只露一条缝。  
**涉及文件**　`electron/renderer/onboarding.js`、`index.html`、`styles.css`、`rich-editor.js`  
**改后效果**　首次启动按推荐线路；插入对话框完整可见。  
**发版**　否，随 **2.0.1** 打包

---

#### 2026-07-13 08:33 — 安装包无法安装 + 进度页排版 · 已测试

**改了什么**　① `CloseApplications=force` ② 轮播文案与 ProgressGauge 同 Parent ③ `[Files]` 排除收紧。  
**为什么改**　Python 占用致用户中止；文案控件坐标系错位出现灰条。  
**涉及文件**　`scripts/installer/YizhiSetup.iss`、`docs/commercial/构建安装包.md`  
**改后效果**　覆盖安装可继续；进度条与金句布局正常。已重编译 Setup（建议对外发 2.0.1）。  
**发版**　是，安装包相关 → **2.0.1**（见文末）

---

#### 2026-07-13 07:31 — 静默启动 Splash + Staging 精简 · 已测试

**改了什么**　① VBS 直启 Electron + Splash bootstrap ② `prune_install_staging.ps1` ③ 用户数据 `%LOCALAPPDATA%\Yizhi`。  
**为什么改**　安装后黑框与 uvicorn 日志；安装包过大。  
**涉及文件**　`YizhiStart.vbs`、`electron/main.js`、`splash.*`、`prune_install_staging.ps1`、`build-portable.ps1`、`patch-installed.ps1`  
**改后效果**　无控制台、有启动动画；staging 文件数减少。  
**发版**　是 → `build-installer.ps1` 或 `patch-installed.ps1`

---

#### 待发布 · 2.0.1（建议）

**版本**　`electron/package.json` → `"2.0.1"`；`yizhi-web/latest.json` 与安装包一致。  
**构建**
```powershell
cd E:\app\Myknowledge
.\scripts\run_commercial_preflight.ps1 -LicenseServer "https://www.yzwhysxx.cn/license" -AdminKey "<KEY>" -JwtSecret "<SECRET>"
.\scripts\build-installer.ps1 -LicenseServer "https://www.yzwhysxx.cn/license" -JwtSecret "<SECRET>"
```
**上传**　`Yizhi-Setup-2.0.1.exe`、`latest.json` → 服务器 `yizhi/`（见 [发布前最后三步.md](commercial/发布前最后三步.md)）。  
**验证**　`curl https://www.yzwhysxx.cn/yizhi/latest.json`；干净机安装：无控制台、Splash、指引、闪念折叠、安装页正常。  
**热补丁 2.0.0**　管理员：`.\scripts\installer\patch-installed.ps1`；renderer 改动须随 2.0.1 或扩展 patch。

---

#### 记录模板（复制到「---」下方顶部）

```markdown
#### YYYY-MM-DD HH:mm — 简短标题 · 已测试

**改了什么**　…  
**为什么改**　…  
**涉及文件**　`path/to/file`  
**改后效果**　…  
**发版**　否，随 x.y.z 打包 / 是 → x.y.z

---
```
